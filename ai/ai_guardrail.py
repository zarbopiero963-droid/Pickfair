"""
AI Guardrail - Protezione automatica per trading AI
"""
import time
import logging
import threading
from dataGuardrailState:\n level: GuardrailLevel = GuardrailLevel.NORMAL\n block_reasons: List[BlockReason] = field(check_error_state(self) -> tuple]:\n with self._lock:\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time < self.state.blocked_until: return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE check_error_state(self) -> tuple[bool, Optional[BlockReason]]:\n with self._lock:\n if self.state.manual_block:\n return False, BlockReason.MANUAL_BLOCK\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time() < self.state.blocked_until:\n return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE set_manual_block(self, enabled: bool = True):\n with self._lock:\n self.state.manual_block = bool(enabled)\n if enabled:\n self.state.level = GuardrailLevel.BLOCKED\n if BlockReason.MANUAL_BLOCK not in self.state.block_reasons:\n self.state.block_reasons.append(BlockReason.MANUAL_BLOCK)\n else:\n self.state.block_reasons = [r for r in self.state.block_reasons if r != BlockReason.MANUAL_BLOCK]\n if not self.state.block_reasons:\n self.state.level = GuardrailLevel.NORMAL\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE is_manually_blocked(self) -> bool:\n with self._lock:\n return bool(self.state.manual_block) ENDREPLACEault_factory=list)\n warnings: List[str] = field(check_error_state(self) -> tuple]:\n with self._lock:\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time < self.state.blocked_until: return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE check_error_state(self) -> tuple[bool, Optional[BlockReason]]:\n with self._lock:\n if self.state.manual_block:\n return False, BlockReason.MANUAL_BLOCK\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time() < self.state.blocked_until:\n return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE set_manual_block(self, enabled: bool = True):\n with self._lock:\n self.state.manual_block = bool(enabled)\n if enabled:\n self.state.level = GuardrailLevel.BLOCKED\n if BlockReason.MANUAL_BLOCK not in self.state.block_reasons:\n self.state.block_reasons.append(BlockReason.MANUAL_BLOCK)\n else:\n self.state.block_reasons = [r for r in self.state.block_reasons if r != BlockReason.MANUAL_BLOCK]\n if not self.state.block_reasons:\n self.state.level = GuardrailLevel.NORMAL\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE is_manually_blocked(self) -> bool:\n with self._lock:\n return bool(self.state.manual_block) ENDREPLACEault_factory=list)\n last_order_time: float = 0.0\n consecutive_errors: int = 0\n blocked_until: float = 0.0\n order_history: List[OrderRecord] = field(check_error_state(self) -> tuple]:\n with self._lock:\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time < self.state.blocked_until: return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE check_error_state(self) -> tuple[bool, Optional[BlockReason]]:\n with self._lock:\n if self.state.manual_block:\n return False, BlockReason.MANUAL_BLOCK\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time() < self.state.blocked_until:\n return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE set_manual_block(self, enabled: bool = True):\n with self._lock:\n self.state.manual_block = bool(enabled)\n if enabled:\n self.state.level = GuardrailLevel.BLOCKED\n if BlockReason.MANUAL_BLOCK not in self.state.block_reasons:\n self.state.block_reasons.append(BlockReason.MANUAL_BLOCK)\n else:\n self.state.block_reasons = [r for r in self.state.block_reasons if r != BlockReason.MANUAL_BLOCK]\n if not self.state.block_reasons:\n self.state.level = GuardrailLevel.NORMAL\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE is_manually_blocked(self) -> bool:\n with self._lock:\n return bool(self.state.manual_block) ENDREPLACEault_factory=list) class GuardrailState:\n level: GuardrailLevel = GuardrailLevel.NORMAL\n block_reasons: List[BlockReason] = field(check_error_state(self) -> tuple]:\n with self._lock:\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time < self.state.blocked_until: return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE check_error_state(self) -> tuple[bool, Optional[BlockReason]]:\n with self._lock:\n if self.state.manual_block:\n return False, BlockReason.MANUAL_BLOCK\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time() < self.state.blocked_until:\n return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE set_manual_block(self, enabled: bool = True):\n with self._lock:\n self.state.manual_block = bool(enabled)\n if enabled:\n self.state.level = GuardrailLevel.BLOCKED\n if BlockReason.MANUAL_BLOCK not in self.state.block_reasons:\n self.state.block_reasons.append(BlockReason.MANUAL_BLOCK)\n else:\n self.state.block_reasons = [r for r in self.state.block_reasons if r != BlockReason.MANUAL_BLOCK]\n if not self.state.block_reasons:\n self.state.level = GuardrailLevel.NORMAL\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE is_manually_blocked(self) -> bool:\n with self._lock:\n return bool(self.state.manual_block) ENDREPLACEault_factory=list)\n warnings: List[str] = field(check_error_state(self) -> tuple]:\n with self._lock:\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time < self.state.blocked_until: return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE check_error_state(self) -> tuple[bool, Optional[BlockReason]]:\n with self._lock:\n if self.state.manual_block:\n return False, BlockReason.MANUAL_BLOCK\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time() < self.state.blocked_until:\n return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE set_manual_block(self, enabled: bool = True):\n with self._lock:\n self.state.manual_block = bool(enabled)\n if enabled:\n self.state.level = GuardrailLevel.BLOCKED\n if BlockReason.MANUAL_BLOCK not in self.state.block_reasons:\n self.state.block_reasons.append(BlockReason.MANUAL_BLOCK)\n else:\n self.state.block_reasons = [r for r in self.state.block_reasons if r != BlockReason.MANUAL_BLOCK]\n if not self.state.block_reasons:\n self.state.level = GuardrailLevel.NORMAL\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE is_manually_blocked(self) -> bool:\n with self._lock:\n return bool(self.state.manual_block) ENDREPLACEault_factory=list)\n last_order_time: float = 0.0\n consecutive_errors: int = 0\n blocked_until: float = 0.0\n manual_block: bool = False\n order_history: List[OrderRecord] = field(check_error_state(self) -> tuple]:\n with self._lock:\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time < self.state.blocked_until: return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE check_error_state(self) -> tuple[bool, Optional[BlockReason]]:\n with self._lock:\n if self.state.manual_block:\n return False, BlockReason.MANUAL_BLOCK\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time() < self.state.blocked_until:\n return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE set_manual_block(self, enabled: bool = True):\n with self._lock:\n self.state.manual_block = bool(enabled)\n if enabled:\n self.state.level = GuardrailLevel.BLOCKED\n if BlockReason.MANUAL_BLOCK not in self.state.block_reasons:\n self.state.block_reasons.append(BlockReason.MANUAL_BLOCK)\n else:\n self.state.block_reasons = [r for r in self.state.block_reasons if r != BlockReason.MANUAL_BLOCK]\n if not self.state.block_reasons:\n self.state.level = GuardrailLevel.NORMAL\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE is_manually_blocked(self) -> bool:\n with self._lock:\n return bool(self.state.manual_block) ENDREPLACEault_factory=list) ENDREPLACEes import dataGuardrailState:\n level: GuardrailLevel = GuardrailLevel.NORMAL\n block_reasons: List[BlockReason] = field(check_error_state(self) -> tuple]:\n with self._lock:\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time < self.state.blocked_until: return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE check_error_state(self) -> tuple[bool, Optional[BlockReason]]:\n with self._lock:\n if self.state.manual_block:\n return False, BlockReason.MANUAL_BLOCK\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time() < self.state.blocked_until:\n return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE set_manual_block(self, enabled: bool = True):\n with self._lock:\n self.state.manual_block = bool(enabled)\n if enabled:\n self.state.level = GuardrailLevel.BLOCKED\n if BlockReason.MANUAL_BLOCK not in self.state.block_reasons:\n self.state.block_reasons.append(BlockReason.MANUAL_BLOCK)\n else:\n self.state.block_reasons = [r for r in self.state.block_reasons if r != BlockReason.MANUAL_BLOCK]\n if not self.state.block_reasons:\n self.state.level = GuardrailLevel.NORMAL\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE is_manually_blocked(self) -> bool:\n with self._lock:\n return bool(self.state.manual_block) ENDREPLACEault_factory=list)\n warnings: List[str] = field(check_error_state(self) -> tuple]:\n with self._lock:\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time < self.state.blocked_until: return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE check_error_state(self) -> tuple[bool, Optional[BlockReason]]:\n with self._lock:\n if self.state.manual_block:\n return False, BlockReason.MANUAL_BLOCK\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time() < self.state.blocked_until:\n return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE set_manual_block(self, enabled: bool = True):\n with self._lock:\n self.state.manual_block = bool(enabled)\n if enabled:\n self.state.level = GuardrailLevel.BLOCKED\n if BlockReason.MANUAL_BLOCK not in self.state.block_reasons:\n self.state.block_reasons.append(BlockReason.MANUAL_BLOCK)\n else:\n self.state.block_reasons = [r for r in self.state.block_reasons if r != BlockReason.MANUAL_BLOCK]\n if not self.state.block_reasons:\n self.state.level = GuardrailLevel.NORMAL\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE is_manually_blocked(self) -> bool:\n with self._lock:\n return bool(self.state.manual_block) ENDREPLACEault_factory=list)\n last_order_time: float = 0.0\n consecutive_errors: int = 0\n blocked_until: float = 0.0\n order_history: List[OrderRecord] = field(check_error_state(self) -> tuple]:\n with self._lock:\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time < self.state.blocked_until: return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE check_error_state(self) -> tuple[bool, Optional[BlockReason]]:\n with self._lock:\n if self.state.manual_block:\n return False, BlockReason.MANUAL_BLOCK\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time() < self.state.blocked_until:\n return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE set_manual_block(self, enabled: bool = True):\n with self._lock:\n self.state.manual_block = bool(enabled)\n if enabled:\n self.state.level = GuardrailLevel.BLOCKED\n if BlockReason.MANUAL_BLOCK not in self.state.block_reasons:\n self.state.block_reasons.append(BlockReason.MANUAL_BLOCK)\n else:\n self.state.block_reasons = [r for r in self.state.block_reasons if r != BlockReason.MANUAL_BLOCK]\n if not self.state.block_reasons:\n self.state.level = GuardrailLevel.NORMAL\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE is_manually_blocked(self) -> bool:\n with self._lock:\n return bool(self.state.manual_block) ENDREPLACEault_factory=list) class GuardrailState:\n level: GuardrailLevel = GuardrailLevel.NORMAL\n block_reasons: List[BlockReason] = field(check_error_state(self) -> tuple]:\n with self._lock:\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time < self.state.blocked_until: return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE check_error_state(self) -> tuple[bool, Optional[BlockReason]]:\n with self._lock:\n if self.state.manual_block:\n return False, BlockReason.MANUAL_BLOCK\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time() < self.state.blocked_until:\n return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE set_manual_block(self, enabled: bool = True):\n with self._lock:\n self.state.manual_block = bool(enabled)\n if enabled:\n self.state.level = GuardrailLevel.BLOCKED\n if BlockReason.MANUAL_BLOCK not in self.state.block_reasons:\n self.state.block_reasons.append(BlockReason.MANUAL_BLOCK)\n else:\n self.state.block_reasons = [r for r in self.state.block_reasons if r != BlockReason.MANUAL_BLOCK]\n if not self.state.block_reasons:\n self.state.level = GuardrailLevel.NORMAL\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE is_manually_blocked(self) -> bool:\n with self._lock:\n return bool(self.state.manual_block) ENDREPLACEault_factory=list)\n warnings: List[str] = field(check_error_state(self) -> tuple]:\n with self._lock:\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time < self.state.blocked_until: return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE check_error_state(self) -> tuple[bool, Optional[BlockReason]]:\n with self._lock:\n if self.state.manual_block:\n return False, BlockReason.MANUAL_BLOCK\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time() < self.state.blocked_until:\n return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE set_manual_block(self, enabled: bool = True):\n with self._lock:\n self.state.manual_block = bool(enabled)\n if enabled:\n self.state.level = GuardrailLevel.BLOCKED\n if BlockReason.MANUAL_BLOCK not in self.state.block_reasons:\n self.state.block_reasons.append(BlockReason.MANUAL_BLOCK)\n else:\n self.state.block_reasons = [r for r in self.state.block_reasons if r != BlockReason.MANUAL_BLOCK]\n if not self.state.block_reasons:\n self.state.level = GuardrailLevel.NORMAL\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE is_manually_blocked(self) -> bool:\n with self._lock:\n return bool(self.state.manual_block) ENDREPLACEault_factory=list)\n last_order_time: float = 0.0\n consecutive_errors: int = 0\n blocked_until: float = 0.0\n manual_block: bool = False\n order_history: List[OrderRecord] = field(check_error_state(self) -> tuple]:\n with self._lock:\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time < self.state.blocked_until: return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE check_error_state(self) -> tuple[bool, Optional[BlockReason]]:\n with self._lock:\n if self.state.manual_block:\n return False, BlockReason.MANUAL_BLOCK\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time() < self.state.blocked_until:\n return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE set_manual_block(self, enabled: bool = True):\n with self._lock:\n self.state.manual_block = bool(enabled)\n if enabled:\n self.state.level = GuardrailLevel.BLOCKED\n if BlockReason.MANUAL_BLOCK not in self.state.block_reasons:\n self.state.block_reasons.append(BlockReason.MANUAL_BLOCK)\n else:\n self.state.block_reasons = [r for r in self.state.block_reasons if r != BlockReason.MANUAL_BLOCK]\n if not self.state.block_reasons:\n self.state.level = GuardrailLevel.NORMAL\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE is_manually_blocked(self) -> bool:\n with self._lock:\n return bool(self.state.manual_block) ENDREPLACEault_factory=list) ENDREPLACE, field
from typing import Dict, List, Optional, Set
from enum import Enum

logger = logging.getLogger(__name__)

GuardrailState:\n level: GuardrailLevel = GuardrailLevel.NORMAL\n block_reasons: List[BlockReason] = field(check_error_state(self) -> tuple]:\n with self._lock:\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time < self.state.blocked_until: return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE check_error_state(self) -> tuple[bool, Optional[BlockReason]]:\n with self._lock:\n if self.state.manual_block:\n return False, BlockReason.MANUAL_BLOCK\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time() < self.state.blocked_until:\n return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE set_manual_block(self, enabled: bool = True):\n with self._lock:\n self.state.manual_block = bool(enabled)\n if enabled:\n self.state.level = GuardrailLevel.BLOCKED\n if BlockReason.MANUAL_BLOCK not in self.state.block_reasons:\n self.state.block_reasons.append(BlockReason.MANUAL_BLOCK)\n else:\n self.state.block_reasons = [r for r in self.state.block_reasons if r != BlockReason.MANUAL_BLOCK]\n if not self.state.block_reasons:\n self.state.level = GuardrailLevel.NORMAL\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE is_manually_blocked(self) -> bool:\n with self._lock:\n return bool(self.state.manual_block) ENDREPLACEault_factory=list)\n warnings: List[str] = field(check_error_state(self) -> tuple]:\n with self._lock:\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time < self.state.blocked_until: return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE check_error_state(self) -> tuple[bool, Optional[BlockReason]]:\n with self._lock:\n if self.state.manual_block:\n return False, BlockReason.MANUAL_BLOCK\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time() < self.state.blocked_until:\n return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE set_manual_block(self, enabled: bool = True):\n with self._lock:\n self.state.manual_block = bool(enabled)\n if enabled:\n self.state.level = GuardrailLevel.BLOCKED\n if BlockReason.MANUAL_BLOCK not in self.state.block_reasons:\n self.state.block_reasons.append(BlockReason.MANUAL_BLOCK)\n else:\n self.state.block_reasons = [r for r in self.state.block_reasons if r != BlockReason.MANUAL_BLOCK]\n if not self.state.block_reasons:\n self.state.level = GuardrailLevel.NORMAL\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE is_manually_blocked(self) -> bool:\n with self._lock:\n return bool(self.state.manual_block) ENDREPLACEault_factory=list)\n last_order_time: float = 0.0\n consecutive_errors: int = 0\n blocked_until: float = 0.0\n order_history: List[OrderRecord] = field(check_error_state(self) -> tuple]:\n with self._lock:\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time < self.state.blocked_until: return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE check_error_state(self) -> tuple[bool, Optional[BlockReason]]:\n with self._lock:\n if self.state.manual_block:\n return False, BlockReason.MANUAL_BLOCK\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time() < self.state.blocked_until:\n return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE set_manual_block(self, enabled: bool = True):\n with self._lock:\n self.state.manual_block = bool(enabled)\n if enabled:\n self.state.level = GuardrailLevel.BLOCKED\n if BlockReason.MANUAL_BLOCK not in self.state.block_reasons:\n self.state.block_reasons.append(BlockReason.MANUAL_BLOCK)\n else:\n self.state.block_reasons = [r for r in self.state.block_reasons if r != BlockReason.MANUAL_BLOCK]\n if not self.state.block_reasons:\n self.state.level = GuardrailLevel.NORMAL\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE is_manually_blocked(self) -> bool:\n with self._lock:\n return bool(self.state.manual_block) ENDREPLACEault_factory=list) class GuardrailState:\n level: GuardrailLevel = GuardrailLevel.NORMAL\n block_reasons: List[BlockReason] = field(check_error_state(self) -> tuple]:\n with self._lock:\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time < self.state.blocked_until: return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE check_error_state(self) -> tuple[bool, Optional[BlockReason]]:\n with self._lock:\n if self.state.manual_block:\n return False, BlockReason.MANUAL_BLOCK\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time() < self.state.blocked_until:\n return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE set_manual_block(self, enabled: bool = True):\n with self._lock:\n self.state.manual_block = bool(enabled)\n if enabled:\n self.state.level = GuardrailLevel.BLOCKED\n if BlockReason.MANUAL_BLOCK not in self.state.block_reasons:\n self.state.block_reasons.append(BlockReason.MANUAL_BLOCK)\n else:\n self.state.block_reasons = [r for r in self.state.block_reasons if r != BlockReason.MANUAL_BLOCK]\n if not self.state.block_reasons:\n self.state.level = GuardrailLevel.NORMAL\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE is_manually_blocked(self) -> bool:\n with self._lock:\n return bool(self.state.manual_block) ENDREPLACEault_factory=list)\n warnings: List[str] = field(check_error_state(self) -> tuple]:\n with self._lock:\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time < self.state.blocked_until: return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE check_error_state(self) -> tuple[bool, Optional[BlockReason]]:\n with self._lock:\n if self.state.manual_block:\n return False, BlockReason.MANUAL_BLOCK\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time() < self.state.blocked_until:\n return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE set_manual_block(self, enabled: bool = True):\n with self._lock:\n self.state.manual_block = bool(enabled)\n if enabled:\n self.state.level = GuardrailLevel.BLOCKED\n if BlockReason.MANUAL_BLOCK not in self.state.block_reasons:\n self.state.block_reasons.append(BlockReason.MANUAL_BLOCK)\n else:\n self.state.block_reasons = [r for r in self.state.block_reasons if r != BlockReason.MANUAL_BLOCK]\n if not self.state.block_reasons:\n self.state.level = GuardrailLevel.NORMAL\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE is_manually_blocked(self) -> bool:\n with self._lock:\n return bool(self.state.manual_block) ENDREPLACEault_factory=list)\n last_order_time: float = 0.0\n consecutive_errors: int = 0\n blocked_until: float = 0.0\n manual_block: bool = False\n order_history: List[OrderRecord] = field(check_error_state(self) -> tuple]:\n with self._lock:\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time < self.state.blocked_until: return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE check_error_state(self) -> tuple[bool, Optional[BlockReason]]:\n with self._lock:\n if self.state.manual_block:\n return False, BlockReason.MANUAL_BLOCK\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time() < self.state.blocked_until:\n return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE set_manual_block(self, enabled: bool = True):\n with self._lock:\n self.state.manual_block = bool(enabled)\n if enabled:\n self.state.level = GuardrailLevel.BLOCKED\n if BlockReason.MANUAL_BLOCK not in self.state.block_reasons:\n self.state.block_reasons.append(BlockReason.MANUAL_BLOCK)\n else:\n self.state.block_reasons = [r for r in self.state.block_reasons if r != BlockReason.MANUAL_BLOCK]\n if not self.state.block_reasons:\n self.state.level = GuardrailLevel.NORMAL\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE is_manually_blocked(self) -> bool:\n with self._lock:\n return bool(self.state.manual_block) ENDREPLACEault_factory=list) ENDREPLACE GuardrailLevel(Enum):
    NORMAL = "normal"
    WARNING = "warning"
    BLOCKED = "blocked"

GuardrailState:\n level: GuardrailLevel = GuardrailLevel.NORMAL\n block_reasons: List[BlockReason] = field(check_error_state(self) -> tuple]:\n with self._lock:\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time < self.state.blocked_until: return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE check_error_state(self) -> tuple[bool, Optional[BlockReason]]:\n with self._lock:\n if self.state.manual_block:\n return False, BlockReason.MANUAL_BLOCK\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time() < self.state.blocked_until:\n return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE set_manual_block(self, enabled: bool = True):\n with self._lock:\n self.state.manual_block = bool(enabled)\n if enabled:\n self.state.level = GuardrailLevel.BLOCKED\n if BlockReason.MANUAL_BLOCK not in self.state.block_reasons:\n self.state.block_reasons.append(BlockReason.MANUAL_BLOCK)\n else:\n self.state.block_reasons = [r for r in self.state.block_reasons if r != BlockReason.MANUAL_BLOCK]\n if not self.state.block_reasons:\n self.state.level = GuardrailLevel.NORMAL\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE is_manually_blocked(self) -> bool:\n with self._lock:\n return bool(self.state.manual_block) ENDREPLACEault_factory=list)\n warnings: List[str] = field(check_error_state(self) -> tuple]:\n with self._lock:\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time < self.state.blocked_until: return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE check_error_state(self) -> tuple[bool, Optional[BlockReason]]:\n with self._lock:\n if self.state.manual_block:\n return False, BlockReason.MANUAL_BLOCK\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time() < self.state.blocked_until:\n return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE set_manual_block(self, enabled: bool = True):\n with self._lock:\n self.state.manual_block = bool(enabled)\n if enabled:\n self.state.level = GuardrailLevel.BLOCKED\n if BlockReason.MANUAL_BLOCK not in self.state.block_reasons:\n self.state.block_reasons.append(BlockReason.MANUAL_BLOCK)\n else:\n self.state.block_reasons = [r for r in self.state.block_reasons if r != BlockReason.MANUAL_BLOCK]\n if not self.state.block_reasons:\n self.state.level = GuardrailLevel.NORMAL\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE is_manually_blocked(self) -> bool:\n with self._lock:\n return bool(self.state.manual_block) ENDREPLACEault_factory=list)\n last_order_time: float = 0.0\n consecutive_errors: int = 0\n blocked_until: float = 0.0\n order_history: List[OrderRecord] = field(check_error_state(self) -> tuple]:\n with self._lock:\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time < self.state.blocked_until: return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE check_error_state(self) -> tuple[bool, Optional[BlockReason]]:\n with self._lock:\n if self.state.manual_block:\n return False, BlockReason.MANUAL_BLOCK\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time() < self.state.blocked_until:\n return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE set_manual_block(self, enabled: bool = True):\n with self._lock:\n self.state.manual_block = bool(enabled)\n if enabled:\n self.state.level = GuardrailLevel.BLOCKED\n if BlockReason.MANUAL_BLOCK not in self.state.block_reasons:\n self.state.block_reasons.append(BlockReason.MANUAL_BLOCK)\n else:\n self.state.block_reasons = [r for r in self.state.block_reasons if r != BlockReason.MANUAL_BLOCK]\n if not self.state.block_reasons:\n self.state.level = GuardrailLevel.NORMAL\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE is_manually_blocked(self) -> bool:\n with self._lock:\n return bool(self.state.manual_block) ENDREPLACEault_factory=list) class GuardrailState:\n level: GuardrailLevel = GuardrailLevel.NORMAL\n block_reasons: List[BlockReason] = field(check_error_state(self) -> tuple]:\n with self._lock:\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time < self.state.blocked_until: return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE check_error_state(self) -> tuple[bool, Optional[BlockReason]]:\n with self._lock:\n if self.state.manual_block:\n return False, BlockReason.MANUAL_BLOCK\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time() < self.state.blocked_until:\n return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE set_manual_block(self, enabled: bool = True):\n with self._lock:\n self.state.manual_block = bool(enabled)\n if enabled:\n self.state.level = GuardrailLevel.BLOCKED\n if BlockReason.MANUAL_BLOCK not in self.state.block_reasons:\n self.state.block_reasons.append(BlockReason.MANUAL_BLOCK)\n else:\n self.state.block_reasons = [r for r in self.state.block_reasons if r != BlockReason.MANUAL_BLOCK]\n if not self.state.block_reasons:\n self.state.level = GuardrailLevel.NORMAL\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE is_manually_blocked(self) -> bool:\n with self._lock:\n return bool(self.state.manual_block) ENDREPLACEault_factory=list)\n warnings: List[str] = field(check_error_state(self) -> tuple]:\n with self._lock:\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time < self.state.blocked_until: return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE check_error_state(self) -> tuple[bool, Optional[BlockReason]]:\n with self._lock:\n if self.state.manual_block:\n return False, BlockReason.MANUAL_BLOCK\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time() < self.state.blocked_until:\n return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE set_manual_block(self, enabled: bool = True):\n with self._lock:\n self.state.manual_block = bool(enabled)\n if enabled:\n self.state.level = GuardrailLevel.BLOCKED\n if BlockReason.MANUAL_BLOCK not in self.state.block_reasons:\n self.state.block_reasons.append(BlockReason.MANUAL_BLOCK)\n else:\n self.state.block_reasons = [r for r in self.state.block_reasons if r != BlockReason.MANUAL_BLOCK]\n if not self.state.block_reasons:\n self.state.level = GuardrailLevel.NORMAL\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE is_manually_blocked(self) -> bool:\n with self._lock:\n return bool(self.state.manual_block) ENDREPLACEault_factory=list)\n last_order_time: float = 0.0\n consecutive_errors: int = 0\n blocked_until: float = 0.0\n manual_block: bool = False\n order_history: List[OrderRecord] = field(check_error_state(self) -> tuple]:\n with self._lock:\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time < self.state.blocked_until: return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE check_error_state(self) -> tuple[bool, Optional[BlockReason]]:\n with self._lock:\n if self.state.manual_block:\n return False, BlockReason.MANUAL_BLOCK\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time() < self.state.blocked_until:\n return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE set_manual_block(self, enabled: bool = True):\n with self._lock:\n self.state.manual_block = bool(enabled)\n if enabled:\n self.state.level = GuardrailLevel.BLOCKED\n if BlockReason.MANUAL_BLOCK not in self.state.block_reasons:\n self.state.block_reasons.append(BlockReason.MANUAL_BLOCK)\n else:\n self.state.block_reasons = [r for r in self.state.block_reasons if r != BlockReason.MANUAL_BLOCK]\n if not self.state.block_reasons:\n self.state.level = GuardrailLevel.NORMAL\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE is_manually_blocked(self) -> bool:\n with self._lock:\n return bool(self.state.manual_block) ENDREPLACEault_factory=list) ENDREPLACE BlockReason(Enum):
    MARKET_NOT_READY = "market_not_ready"
    HIGH_VOLATILITY = "high_volatility"
    OVERTRADE_PROTECTION = "overtrade_protection"
    CONSECUTIVE_ERRORS = "consecutive_errors"
    INSUFFICIENT_DATA = "insufficient_data"
    GRACE_PERIOD = "grace_period"
    MANUAL_BLOCK = "manual_block"

@dataGuardrailState:\n level: GuardrailLevel = GuardrailLevel.NORMAL\n block_reasons: List[BlockReason] = field(check_error_state(self) -> tuple]:\n with self._lock:\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time < self.state.blocked_until: return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE check_error_state(self) -> tuple[bool, Optional[BlockReason]]:\n with self._lock:\n if self.state.manual_block:\n return False, BlockReason.MANUAL_BLOCK\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time() < self.state.blocked_until:\n return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE set_manual_block(self, enabled: bool = True):\n with self._lock:\n self.state.manual_block = bool(enabled)\n if enabled:\n self.state.level = GuardrailLevel.BLOCKED\n if BlockReason.MANUAL_BLOCK not in self.state.block_reasons:\n self.state.block_reasons.append(BlockReason.MANUAL_BLOCK)\n else:\n self.state.block_reasons = [r for r in self.state.block_reasons if r != BlockReason.MANUAL_BLOCK]\n if not self.state.block_reasons:\n self.state.level = GuardrailLevel.NORMAL\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE is_manually_blocked(self) -> bool:\n with self._lock:\n return bool(self.state.manual_block) ENDREPLACEault_factory=list)\n warnings: List[str] = field(check_error_state(self) -> tuple]:\n with self._lock:\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time < self.state.blocked_until: return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE check_error_state(self) -> tuple[bool, Optional[BlockReason]]:\n with self._lock:\n if self.state.manual_block:\n return False, BlockReason.MANUAL_BLOCK\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time() < self.state.blocked_until:\n return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE set_manual_block(self, enabled: bool = True):\n with self._lock:\n self.state.manual_block = bool(enabled)\n if enabled:\n self.state.level = GuardrailLevel.BLOCKED\n if BlockReason.MANUAL_BLOCK not in self.state.block_reasons:\n self.state.block_reasons.append(BlockReason.MANUAL_BLOCK)\n else:\n self.state.block_reasons = [r for r in self.state.block_reasons if r != BlockReason.MANUAL_BLOCK]\n if not self.state.block_reasons:\n self.state.level = GuardrailLevel.NORMAL\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE is_manually_blocked(self) -> bool:\n with self._lock:\n return bool(self.state.manual_block) ENDREPLACEault_factory=list)\n last_order_time: float = 0.0\n consecutive_errors: int = 0\n blocked_until: float = 0.0\n order_history: List[OrderRecord] = field(check_error_state(self) -> tuple]:\n with self._lock:\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time < self.state.blocked_until: return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE check_error_state(self) -> tuple[bool, Optional[BlockReason]]:\n with self._lock:\n if self.state.manual_block:\n return False, BlockReason.MANUAL_BLOCK\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time() < self.state.blocked_until:\n return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE set_manual_block(self, enabled: bool = True):\n with self._lock:\n self.state.manual_block = bool(enabled)\n if enabled:\n self.state.level = GuardrailLevel.BLOCKED\n if BlockReason.MANUAL_BLOCK not in self.state.block_reasons:\n self.state.block_reasons.append(BlockReason.MANUAL_BLOCK)\n else:\n self.state.block_reasons = [r for r in self.state.block_reasons if r != BlockReason.MANUAL_BLOCK]\n if not self.state.block_reasons:\n self.state.level = GuardrailLevel.NORMAL\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE is_manually_blocked(self) -> bool:\n with self._lock:\n return bool(self.state.manual_block) ENDREPLACEault_factory=list) class GuardrailState:\n level: GuardrailLevel = GuardrailLevel.NORMAL\n block_reasons: List[BlockReason] = field(check_error_state(self) -> tuple]:\n with self._lock:\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time < self.state.blocked_until: return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE check_error_state(self) -> tuple[bool, Optional[BlockReason]]:\n with self._lock:\n if self.state.manual_block:\n return False, BlockReason.MANUAL_BLOCK\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time() < self.state.blocked_until:\n return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE set_manual_block(self, enabled: bool = True):\n with self._lock:\n self.state.manual_block = bool(enabled)\n if enabled:\n self.state.level = GuardrailLevel.BLOCKED\n if BlockReason.MANUAL_BLOCK not in self.state.block_reasons:\n self.state.block_reasons.append(BlockReason.MANUAL_BLOCK)\n else:\n self.state.block_reasons = [r for r in self.state.block_reasons if r != BlockReason.MANUAL_BLOCK]\n if not self.state.block_reasons:\n self.state.level = GuardrailLevel.NORMAL\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE is_manually_blocked(self) -> bool:\n with self._lock:\n return bool(self.state.manual_block) ENDREPLACEault_factory=list)\n warnings: List[str] = field(check_error_state(self) -> tuple]:\n with self._lock:\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time < self.state.blocked_until: return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE check_error_state(self) -> tuple[bool, Optional[BlockReason]]:\n with self._lock:\n if self.state.manual_block:\n return False, BlockReason.MANUAL_BLOCK\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time() < self.state.blocked_until:\n return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE set_manual_block(self, enabled: bool = True):\n with self._lock:\n self.state.manual_block = bool(enabled)\n if enabled:\n self.state.level = GuardrailLevel.BLOCKED\n if BlockReason.MANUAL_BLOCK not in self.state.block_reasons:\n self.state.block_reasons.append(BlockReason.MANUAL_BLOCK)\n else:\n self.state.block_reasons = [r for r in self.state.block_reasons if r != BlockReason.MANUAL_BLOCK]\n if not self.state.block_reasons:\n self.state.level = GuardrailLevel.NORMAL\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE is_manually_blocked(self) -> bool:\n with self._lock:\n return bool(self.state.manual_block) ENDREPLACEault_factory=list)\n last_order_time: float = 0.0\n consecutive_errors: int = 0\n blocked_until: float = 0.0\n manual_block: bool = False\n order_history: List[OrderRecord] = field(check_error_state(self) -> tuple]:\n with self._lock:\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time < self.state.blocked_until: return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE check_error_state(self) -> tuple[bool, Optional[BlockReason]]:\n with self._lock:\n if self.state.manual_block:\n return False, BlockReason.MANUAL_BLOCK\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time() < self.state.blocked_until:\n return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE set_manual_block(self, enabled: bool = True):\n with self._lock:\n self.state.manual_block = bool(enabled)\n if enabled:\n self.state.level = GuardrailLevel.BLOCKED\n if BlockReason.MANUAL_BLOCK not in self.state.block_reasons:\n self.state.block_reasons.append(BlockReason.MANUAL_BLOCK)\n else:\n self.state.block_reasons = [r for r in self.state.block_reasons if r != BlockReason.MANUAL_BLOCK]\n if not self.state.block_reasons:\n self.state.level = GuardrailLevel.NORMAL\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE is_manually_blocked(self) -> bool:\n with self._lock:\n return bool(self.state.manual_block) ENDREPLACEault_factory=list) ENDREPLACE
GuardrailState:\n level: GuardrailLevel = GuardrailLevel.NORMAL\n block_reasons: List[BlockReason] = field(check_error_state(self) -> tuple]:\n with self._lock:\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time < self.state.blocked_until: return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE check_error_state(self) -> tuple[bool, Optional[BlockReason]]:\n with self._lock:\n if self.state.manual_block:\n return False, BlockReason.MANUAL_BLOCK\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time() < self.state.blocked_until:\n return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE set_manual_block(self, enabled: bool = True):\n with self._lock:\n self.state.manual_block = bool(enabled)\n if enabled:\n self.state.level = GuardrailLevel.BLOCKED\n if BlockReason.MANUAL_BLOCK not in self.state.block_reasons:\n self.state.block_reasons.append(BlockReason.MANUAL_BLOCK)\n else:\n self.state.block_reasons = [r for r in self.state.block_reasons if r != BlockReason.MANUAL_BLOCK]\n if not self.state.block_reasons:\n self.state.level = GuardrailLevel.NORMAL\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE is_manually_blocked(self) -> bool:\n with self._lock:\n return bool(self.state.manual_block) ENDREPLACEault_factory=list)\n warnings: List[str] = field(check_error_state(self) -> tuple]:\n with self._lock:\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time < self.state.blocked_until: return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE check_error_state(self) -> tuple[bool, Optional[BlockReason]]:\n with self._lock:\n if self.state.manual_block:\n return False, BlockReason.MANUAL_BLOCK\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time() < self.state.blocked_until:\n return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE set_manual_block(self, enabled: bool = True):\n with self._lock:\n self.state.manual_block = bool(enabled)\n if enabled:\n self.state.level = GuardrailLevel.BLOCKED\n if BlockReason.MANUAL_BLOCK not in self.state.block_reasons:\n self.state.block_reasons.append(BlockReason.MANUAL_BLOCK)\n else:\n self.state.block_reasons = [r for r in self.state.block_reasons if r != BlockReason.MANUAL_BLOCK]\n if not self.state.block_reasons:\n self.state.level = GuardrailLevel.NORMAL\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE is_manually_blocked(self) -> bool:\n with self._lock:\n return bool(self.state.manual_block) ENDREPLACEault_factory=list)\n last_order_time: float = 0.0\n consecutive_errors: int = 0\n blocked_until: float = 0.0\n order_history: List[OrderRecord] = field(check_error_state(self) -> tuple]:\n with self._lock:\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time < self.state.blocked_until: return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE check_error_state(self) -> tuple[bool, Optional[BlockReason]]:\n with self._lock:\n if self.state.manual_block:\n return False, BlockReason.MANUAL_BLOCK\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time() < self.state.blocked_until:\n return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE set_manual_block(self, enabled: bool = True):\n with self._lock:\n self.state.manual_block = bool(enabled)\n if enabled:\n self.state.level = GuardrailLevel.BLOCKED\n if BlockReason.MANUAL_BLOCK not in self.state.block_reasons:\n self.state.block_reasons.append(BlockReason.MANUAL_BLOCK)\n else:\n self.state.block_reasons = [r for r in self.state.block_reasons if r != BlockReason.MANUAL_BLOCK]\n if not self.state.block_reasons:\n self.state.level = GuardrailLevel.NORMAL\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE is_manually_blocked(self) -> bool:\n with self._lock:\n return bool(self.state.manual_block) ENDREPLACEault_factory=list) class GuardrailState:\n level: GuardrailLevel = GuardrailLevel.NORMAL\n block_reasons: List[BlockReason] = field(check_error_state(self) -> tuple]:\n with self._lock:\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time < self.state.blocked_until: return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE check_error_state(self) -> tuple[bool, Optional[BlockReason]]:\n with self._lock:\n if self.state.manual_block:\n return False, BlockReason.MANUAL_BLOCK\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time() < self.state.blocked_until:\n return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE set_manual_block(self, enabled: bool = True):\n with self._lock:\n self.state.manual_block = bool(enabled)\n if enabled:\n self.state.level = GuardrailLevel.BLOCKED\n if BlockReason.MANUAL_BLOCK not in self.state.block_reasons:\n self.state.block_reasons.append(BlockReason.MANUAL_BLOCK)\n else:\n self.state.block_reasons = [r for r in self.state.block_reasons if r != BlockReason.MANUAL_BLOCK]\n if not self.state.block_reasons:\n self.state.level = GuardrailLevel.NORMAL\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE is_manually_blocked(self) -> bool:\n with self._lock:\n return bool(self.state.manual_block) ENDREPLACEault_factory=list)\n warnings: List[str] = field(check_error_state(self) -> tuple]:\n with self._lock:\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time < self.state.blocked_until: return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE check_error_state(self) -> tuple[bool, Optional[BlockReason]]:\n with self._lock:\n if self.state.manual_block:\n return False, BlockReason.MANUAL_BLOCK\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time() < self.state.blocked_until:\n return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE set_manual_block(self, enabled: bool = True):\n with self._lock:\n self.state.manual_block = bool(enabled)\n if enabled:\n self.state.level = GuardrailLevel.BLOCKED\n if BlockReason.MANUAL_BLOCK not in self.state.block_reasons:\n self.state.block_reasons.append(BlockReason.MANUAL_BLOCK)\n else:\n self.state.block_reasons = [r for r in self.state.block_reasons if r != BlockReason.MANUAL_BLOCK]\n if not self.state.block_reasons:\n self.state.level = GuardrailLevel.NORMAL\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE is_manually_blocked(self) -> bool:\n with self._lock:\n return bool(self.state.manual_block) ENDREPLACEault_factory=list)\n last_order_time: float = 0.0\n consecutive_errors: int = 0\n blocked_until: float = 0.0\n manual_block: bool = False\n order_history: List[OrderRecord] = field(check_error_state(self) -> tuple]:\n with self._lock:\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time < self.state.blocked_until: return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE check_error_state(self) -> tuple[bool, Optional[BlockReason]]:\n with self._lock:\n if self.state.manual_block:\n return False, BlockReason.MANUAL_BLOCK\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time() < self.state.blocked_until:\n return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE set_manual_block(self, enabled: bool = True):\n with self._lock:\n self.state.manual_block = bool(enabled)\n if enabled:\n self.state.level = GuardrailLevel.BLOCKED\n if BlockReason.MANUAL_BLOCK not in self.state.block_reasons:\n self.state.block_reasons.append(BlockReason.MANUAL_BLOCK)\n else:\n self.state.block_reasons = [r for r in self.state.block_reasons if r != BlockReason.MANUAL_BLOCK]\n if not self.state.block_reasons:\n self.state.level = GuardrailLevel.NORMAL\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE is_manually_blocked(self) -> bool:\n with self._lock:\n return bool(self.state.manual_block) ENDREPLACEault_factory=list) ENDREPLACE GuardrailConfig:
    auto_green_grace_sec: float = 3.0
    min_tick_count: int = 10
    max_volatility: float = 0.8
    max_orders_per_minute: int = 10
    consecutive_error_limit: int = 3
    cooldown_after_error_sec: float = 30.0
    min_wom_confidence: float = 0.3
    dutching_ready_market_types: Set[str] = field(check_error_state(self) -> tuple]:\n with self._lock:\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time < self.state.blocked_until: return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE check_error_state(self) -> tuple[bool, Optional[BlockReason]]:\n with self._lock:\n if self.state.manual_block:\n return False, BlockReason.MANUAL_BLOCK\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time() < self.state.blocked_until:\n return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE set_manual_block(self, enabled: bool = True):\n with self._lock:\n self.state.manual_block = bool(enabled)\n if enabled:\n self.state.level = GuardrailLevel.BLOCKED\n if BlockReason.MANUAL_BLOCK not in self.state.block_reasons:\n self.state.block_reasons.append(BlockReason.MANUAL_BLOCK)\n else:\n self.state.block_reasons = [r for r in self.state.block_reasons if r != BlockReason.MANUAL_BLOCK]\n if not self.state.block_reasons:\n self.state.level = GuardrailLevel.NORMAL\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE is_manually_blocked(self) -> bool:\n with self._lock:\n return bool(self.state.manual_block) ENDREPLACEault_factory=lambda: {
        "MATCH_ODDS", "WINNER", "OVER_UNDER_25", "OVER_UNDER_35",
        "CORRECT_SCORE", "HALF_TIME", "BOTH_TEAMS_TO_SCORE"
    })

@dataGuardrailState:\n level: GuardrailLevel = GuardrailLevel.NORMAL\n block_reasons: List[BlockReason] = field(check_error_state(self) -> tuple]:\n with self._lock:\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time < self.state.blocked_until: return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE check_error_state(self) -> tuple[bool, Optional[BlockReason]]:\n with self._lock:\n if self.state.manual_block:\n return False, BlockReason.MANUAL_BLOCK\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time() < self.state.blocked_until:\n return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE set_manual_block(self, enabled: bool = True):\n with self._lock:\n self.state.manual_block = bool(enabled)\n if enabled:\n self.state.level = GuardrailLevel.BLOCKED\n if BlockReason.MANUAL_BLOCK not in self.state.block_reasons:\n self.state.block_reasons.append(BlockReason.MANUAL_BLOCK)\n else:\n self.state.block_reasons = [r for r in self.state.block_reasons if r != BlockReason.MANUAL_BLOCK]\n if not self.state.block_reasons:\n self.state.level = GuardrailLevel.NORMAL\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE is_manually_blocked(self) -> bool:\n with self._lock:\n return bool(self.state.manual_block) ENDREPLACEault_factory=list)\n warnings: List[str] = field(check_error_state(self) -> tuple]:\n with self._lock:\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time < self.state.blocked_until: return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE check_error_state(self) -> tuple[bool, Optional[BlockReason]]:\n with self._lock:\n if self.state.manual_block:\n return False, BlockReason.MANUAL_BLOCK\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time() < self.state.blocked_until:\n return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE set_manual_block(self, enabled: bool = True):\n with self._lock:\n self.state.manual_block = bool(enabled)\n if enabled:\n self.state.level = GuardrailLevel.BLOCKED\n if BlockReason.MANUAL_BLOCK not in self.state.block_reasons:\n self.state.block_reasons.append(BlockReason.MANUAL_BLOCK)\n else:\n self.state.block_reasons = [r for r in self.state.block_reasons if r != BlockReason.MANUAL_BLOCK]\n if not self.state.block_reasons:\n self.state.level = GuardrailLevel.NORMAL\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE is_manually_blocked(self) -> bool:\n with self._lock:\n return bool(self.state.manual_block) ENDREPLACEault_factory=list)\n last_order_time: float = 0.0\n consecutive_errors: int = 0\n blocked_until: float = 0.0\n order_history: List[OrderRecord] = field(check_error_state(self) -> tuple]:\n with self._lock:\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time < self.state.blocked_until: return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE check_error_state(self) -> tuple[bool, Optional[BlockReason]]:\n with self._lock:\n if self.state.manual_block:\n return False, BlockReason.MANUAL_BLOCK\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time() < self.state.blocked_until:\n return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE set_manual_block(self, enabled: bool = True):\n with self._lock:\n self.state.manual_block = bool(enabled)\n if enabled:\n self.state.level = GuardrailLevel.BLOCKED\n if BlockReason.MANUAL_BLOCK not in self.state.block_reasons:\n self.state.block_reasons.append(BlockReason.MANUAL_BLOCK)\n else:\n self.state.block_reasons = [r for r in self.state.block_reasons if r != BlockReason.MANUAL_BLOCK]\n if not self.state.block_reasons:\n self.state.level = GuardrailLevel.NORMAL\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE is_manually_blocked(self) -> bool:\n with self._lock:\n return bool(self.state.manual_block) ENDREPLACEault_factory=list) class GuardrailState:\n level: GuardrailLevel = GuardrailLevel.NORMAL\n block_reasons: List[BlockReason] = field(check_error_state(self) -> tuple]:\n with self._lock:\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time < self.state.blocked_until: return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE check_error_state(self) -> tuple[bool, Optional[BlockReason]]:\n with self._lock:\n if self.state.manual_block:\n return False, BlockReason.MANUAL_BLOCK\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time() < self.state.blocked_until:\n return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE set_manual_block(self, enabled: bool = True):\n with self._lock:\n self.state.manual_block = bool(enabled)\n if enabled:\n self.state.level = GuardrailLevel.BLOCKED\n if BlockReason.MANUAL_BLOCK not in self.state.block_reasons:\n self.state.block_reasons.append(BlockReason.MANUAL_BLOCK)\n else:\n self.state.block_reasons = [r for r in self.state.block_reasons if r != BlockReason.MANUAL_BLOCK]\n if not self.state.block_reasons:\n self.state.level = GuardrailLevel.NORMAL\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE is_manually_blocked(self) -> bool:\n with self._lock:\n return bool(self.state.manual_block) ENDREPLACEault_factory=list)\n warnings: List[str] = field(check_error_state(self) -> tuple]:\n with self._lock:\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time < self.state.blocked_until: return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE check_error_state(self) -> tuple[bool, Optional[BlockReason]]:\n with self._lock:\n if self.state.manual_block:\n return False, BlockReason.MANUAL_BLOCK\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time() < self.state.blocked_until:\n return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE set_manual_block(self, enabled: bool = True):\n with self._lock:\n self.state.manual_block = bool(enabled)\n if enabled:\n self.state.level = GuardrailLevel.BLOCKED\n if BlockReason.MANUAL_BLOCK not in self.state.block_reasons:\n self.state.block_reasons.append(BlockReason.MANUAL_BLOCK)\n else:\n self.state.block_reasons = [r for r in self.state.block_reasons if r != BlockReason.MANUAL_BLOCK]\n if not self.state.block_reasons:\n self.state.level = GuardrailLevel.NORMAL\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE is_manually_blocked(self) -> bool:\n with self._lock:\n return bool(self.state.manual_block) ENDREPLACEault_factory=list)\n last_order_time: float = 0.0\n consecutive_errors: int = 0\n blocked_until: float = 0.0\n manual_block: bool = False\n order_history: List[OrderRecord] = field(check_error_state(self) -> tuple]:\n with self._lock:\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time < self.state.blocked_until: return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE check_error_state(self) -> tuple[bool, Optional[BlockReason]]:\n with self._lock:\n if self.state.manual_block:\n return False, BlockReason.MANUAL_BLOCK\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time() < self.state.blocked_until:\n return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE set_manual_block(self, enabled: bool = True):\n with self._lock:\n self.state.manual_block = bool(enabled)\n if enabled:\n self.state.level = GuardrailLevel.BLOCKED\n if BlockReason.MANUAL_BLOCK not in self.state.block_reasons:\n self.state.block_reasons.append(BlockReason.MANUAL_BLOCK)\n else:\n self.state.block_reasons = [r for r in self.state.block_reasons if r != BlockReason.MANUAL_BLOCK]\n if not self.state.block_reasons:\n self.state.level = GuardrailLevel.NORMAL\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE is_manually_blocked(self) -> bool:\n with self._lock:\n return bool(self.state.manual_block) ENDREPLACEault_factory=list) ENDREPLACE
GuardrailState:\n level: GuardrailLevel = GuardrailLevel.NORMAL\n block_reasons: List[BlockReason] = field(check_error_state(self) -> tuple]:\n with self._lock:\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time < self.state.blocked_until: return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE check_error_state(self) -> tuple[bool, Optional[BlockReason]]:\n with self._lock:\n if self.state.manual_block:\n return False, BlockReason.MANUAL_BLOCK\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time() < self.state.blocked_until:\n return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE set_manual_block(self, enabled: bool = True):\n with self._lock:\n self.state.manual_block = bool(enabled)\n if enabled:\n self.state.level = GuardrailLevel.BLOCKED\n if BlockReason.MANUAL_BLOCK not in self.state.block_reasons:\n self.state.block_reasons.append(BlockReason.MANUAL_BLOCK)\n else:\n self.state.block_reasons = [r for r in self.state.block_reasons if r != BlockReason.MANUAL_BLOCK]\n if not self.state.block_reasons:\n self.state.level = GuardrailLevel.NORMAL\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE is_manually_blocked(self) -> bool:\n with self._lock:\n return bool(self.state.manual_block) ENDREPLACEault_factory=list)\n warnings: List[str] = field(check_error_state(self) -> tuple]:\n with self._lock:\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time < self.state.blocked_until: return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE check_error_state(self) -> tuple[bool, Optional[BlockReason]]:\n with self._lock:\n if self.state.manual_block:\n return False, BlockReason.MANUAL_BLOCK\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time() < self.state.blocked_until:\n return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE set_manual_block(self, enabled: bool = True):\n with self._lock:\n self.state.manual_block = bool(enabled)\n if enabled:\n self.state.level = GuardrailLevel.BLOCKED\n if BlockReason.MANUAL_BLOCK not in self.state.block_reasons:\n self.state.block_reasons.append(BlockReason.MANUAL_BLOCK)\n else:\n self.state.block_reasons = [r for r in self.state.block_reasons if r != BlockReason.MANUAL_BLOCK]\n if not self.state.block_reasons:\n self.state.level = GuardrailLevel.NORMAL\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE is_manually_blocked(self) -> bool:\n with self._lock:\n return bool(self.state.manual_block) ENDREPLACEault_factory=list)\n last_order_time: float = 0.0\n consecutive_errors: int = 0\n blocked_until: float = 0.0\n order_history: List[OrderRecord] = field(check_error_state(self) -> tuple]:\n with self._lock:\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time < self.state.blocked_until: return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE check_error_state(self) -> tuple[bool, Optional[BlockReason]]:\n with self._lock:\n if self.state.manual_block:\n return False, BlockReason.MANUAL_BLOCK\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time() < self.state.blocked_until:\n return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE set_manual_block(self, enabled: bool = True):\n with self._lock:\n self.state.manual_block = bool(enabled)\n if enabled:\n self.state.level = GuardrailLevel.BLOCKED\n if BlockReason.MANUAL_BLOCK not in self.state.block_reasons:\n self.state.block_reasons.append(BlockReason.MANUAL_BLOCK)\n else:\n self.state.block_reasons = [r for r in self.state.block_reasons if r != BlockReason.MANUAL_BLOCK]\n if not self.state.block_reasons:\n self.state.level = GuardrailLevel.NORMAL\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE is_manually_blocked(self) -> bool:\n with self._lock:\n return bool(self.state.manual_block) ENDREPLACEault_factory=list) class GuardrailState:\n level: GuardrailLevel = GuardrailLevel.NORMAL\n block_reasons: List[BlockReason] = field(check_error_state(self) -> tuple]:\n with self._lock:\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time < self.state.blocked_until: return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE check_error_state(self) -> tuple[bool, Optional[BlockReason]]:\n with self._lock:\n if self.state.manual_block:\n return False, BlockReason.MANUAL_BLOCK\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time() < self.state.blocked_until:\n return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE set_manual_block(self, enabled: bool = True):\n with self._lock:\n self.state.manual_block = bool(enabled)\n if enabled:\n self.state.level = GuardrailLevel.BLOCKED\n if BlockReason.MANUAL_BLOCK not in self.state.block_reasons:\n self.state.block_reasons.append(BlockReason.MANUAL_BLOCK)\n else:\n self.state.block_reasons = [r for r in self.state.block_reasons if r != BlockReason.MANUAL_BLOCK]\n if not self.state.block_reasons:\n self.state.level = GuardrailLevel.NORMAL\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE is_manually_blocked(self) -> bool:\n with self._lock:\n return bool(self.state.manual_block) ENDREPLACEault_factory=list)\n warnings: List[str] = field(check_error_state(self) -> tuple]:\n with self._lock:\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time < self.state.blocked_until: return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE check_error_state(self) -> tuple[bool, Optional[BlockReason]]:\n with self._lock:\n if self.state.manual_block:\n return False, BlockReason.MANUAL_BLOCK\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time() < self.state.blocked_until:\n return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE set_manual_block(self, enabled: bool = True):\n with self._lock:\n self.state.manual_block = bool(enabled)\n if enabled:\n self.state.level = GuardrailLevel.BLOCKED\n if BlockReason.MANUAL_BLOCK not in self.state.block_reasons:\n self.state.block_reasons.append(BlockReason.MANUAL_BLOCK)\n else:\n self.state.block_reasons = [r for r in self.state.block_reasons if r != BlockReason.MANUAL_BLOCK]\n if not self.state.block_reasons:\n self.state.level = GuardrailLevel.NORMAL\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE is_manually_blocked(self) -> bool:\n with self._lock:\n return bool(self.state.manual_block) ENDREPLACEault_factory=list)\n last_order_time: float = 0.0\n consecutive_errors: int = 0\n blocked_until: float = 0.0\n manual_block: bool = False\n order_history: List[OrderRecord] = field(check_error_state(self) -> tuple]:\n with self._lock:\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time < self.state.blocked_until: return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE check_error_state(self) -> tuple[bool, Optional[BlockReason]]:\n with self._lock:\n if self.state.manual_block:\n return False, BlockReason.MANUAL_BLOCK\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time() < self.state.blocked_until:\n return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE set_manual_block(self, enabled: bool = True):\n with self._lock:\n self.state.manual_block = bool(enabled)\n if enabled:\n self.state.level = GuardrailLevel.BLOCKED\n if BlockReason.MANUAL_BLOCK not in self.state.block_reasons:\n self.state.block_reasons.append(BlockReason.MANUAL_BLOCK)\n else:\n self.state.block_reasons = [r for r in self.state.block_reasons if r != BlockReason.MANUAL_BLOCK]\n if not self.state.block_reasons:\n self.state.level = GuardrailLevel.NORMAL\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE is_manually_blocked(self) -> bool:\n with self._lock:\n return bool(self.state.manual_block) ENDREPLACEault_factory=list) ENDREPLACE OrderRecord:
    timestamp: float
    market_id: str
    selection_id: int
    side: str
    stake: float
    success: bool = True

@dataGuardrailState:\n level: GuardrailLevel = GuardrailLevel.NORMAL\n block_reasons: List[BlockReason] = field(check_error_state(self) -> tuple]:\n with self._lock:\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time < self.state.blocked_until: return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE check_error_state(self) -> tuple[bool, Optional[BlockReason]]:\n with self._lock:\n if self.state.manual_block:\n return False, BlockReason.MANUAL_BLOCK\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time() < self.state.blocked_until:\n return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE set_manual_block(self, enabled: bool = True):\n with self._lock:\n self.state.manual_block = bool(enabled)\n if enabled:\n self.state.level = GuardrailLevel.BLOCKED\n if BlockReason.MANUAL_BLOCK not in self.state.block_reasons:\n self.state.block_reasons.append(BlockReason.MANUAL_BLOCK)\n else:\n self.state.block_reasons = [r for r in self.state.block_reasons if r != BlockReason.MANUAL_BLOCK]\n if not self.state.block_reasons:\n self.state.level = GuardrailLevel.NORMAL\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE is_manually_blocked(self) -> bool:\n with self._lock:\n return bool(self.state.manual_block) ENDREPLACEault_factory=list)\n warnings: List[str] = field(check_error_state(self) -> tuple]:\n with self._lock:\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time < self.state.blocked_until: return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE check_error_state(self) -> tuple[bool, Optional[BlockReason]]:\n with self._lock:\n if self.state.manual_block:\n return False, BlockReason.MANUAL_BLOCK\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time() < self.state.blocked_until:\n return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE set_manual_block(self, enabled: bool = True):\n with self._lock:\n self.state.manual_block = bool(enabled)\n if enabled:\n self.state.level = GuardrailLevel.BLOCKED\n if BlockReason.MANUAL_BLOCK not in self.state.block_reasons:\n self.state.block_reasons.append(BlockReason.MANUAL_BLOCK)\n else:\n self.state.block_reasons = [r for r in self.state.block_reasons if r != BlockReason.MANUAL_BLOCK]\n if not self.state.block_reasons:\n self.state.level = GuardrailLevel.NORMAL\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE is_manually_blocked(self) -> bool:\n with self._lock:\n return bool(self.state.manual_block) ENDREPLACEault_factory=list)\n last_order_time: float = 0.0\n consecutive_errors: int = 0\n blocked_until: float = 0.0\n order_history: List[OrderRecord] = field(check_error_state(self) -> tuple]:\n with self._lock:\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time < self.state.blocked_until: return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE check_error_state(self) -> tuple[bool, Optional[BlockReason]]:\n with self._lock:\n if self.state.manual_block:\n return False, BlockReason.MANUAL_BLOCK\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time() < self.state.blocked_until:\n return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE set_manual_block(self, enabled: bool = True):\n with self._lock:\n self.state.manual_block = bool(enabled)\n if enabled:\n self.state.level = GuardrailLevel.BLOCKED\n if BlockReason.MANUAL_BLOCK not in self.state.block_reasons:\n self.state.block_reasons.append(BlockReason.MANUAL_BLOCK)\n else:\n self.state.block_reasons = [r for r in self.state.block_reasons if r != BlockReason.MANUAL_BLOCK]\n if not self.state.block_reasons:\n self.state.level = GuardrailLevel.NORMAL\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE is_manually_blocked(self) -> bool:\n with self._lock:\n return bool(self.state.manual_block) ENDREPLACEault_factory=list) class GuardrailState:\n level: GuardrailLevel = GuardrailLevel.NORMAL\n block_reasons: List[BlockReason] = field(check_error_state(self) -> tuple]:\n with self._lock:\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time < self.state.blocked_until: return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE check_error_state(self) -> tuple[bool, Optional[BlockReason]]:\n with self._lock:\n if self.state.manual_block:\n return False, BlockReason.MANUAL_BLOCK\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time() < self.state.blocked_until:\n return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE set_manual_block(self, enabled: bool = True):\n with self._lock:\n self.state.manual_block = bool(enabled)\n if enabled:\n self.state.level = GuardrailLevel.BLOCKED\n if BlockReason.MANUAL_BLOCK not in self.state.block_reasons:\n self.state.block_reasons.append(BlockReason.MANUAL_BLOCK)\n else:\n self.state.block_reasons = [r for r in self.state.block_reasons if r != BlockReason.MANUAL_BLOCK]\n if not self.state.block_reasons:\n self.state.level = GuardrailLevel.NORMAL\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE is_manually_blocked(self) -> bool:\n with self._lock:\n return bool(self.state.manual_block) ENDREPLACEault_factory=list)\n warnings: List[str] = field(check_error_state(self) -> tuple]:\n with self._lock:\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time < self.state.blocked_until: return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE check_error_state(self) -> tuple[bool, Optional[BlockReason]]:\n with self._lock:\n if self.state.manual_block:\n return False, BlockReason.MANUAL_BLOCK\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time() < self.state.blocked_until:\n return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE set_manual_block(self, enabled: bool = True):\n with self._lock:\n self.state.manual_block = bool(enabled)\n if enabled:\n self.state.level = GuardrailLevel.BLOCKED\n if BlockReason.MANUAL_BLOCK not in self.state.block_reasons:\n self.state.block_reasons.append(BlockReason.MANUAL_BLOCK)\n else:\n self.state.block_reasons = [r for r in self.state.block_reasons if r != BlockReason.MANUAL_BLOCK]\n if not self.state.block_reasons:\n self.state.level = GuardrailLevel.NORMAL\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE is_manually_blocked(self) -> bool:\n with self._lock:\n return bool(self.state.manual_block) ENDREPLACEault_factory=list)\n last_order_time: float = 0.0\n consecutive_errors: int = 0\n blocked_until: float = 0.0\n manual_block: bool = False\n order_history: List[OrderRecord] = field(check_error_state(self) -> tuple]:\n with self._lock:\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time < self.state.blocked_until: return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE check_error_state(self) -> tuple[bool, Optional[BlockReason]]:\n with self._lock:\n if self.state.manual_block:\n return False, BlockReason.MANUAL_BLOCK\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time() < self.state.blocked_until:\n return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE set_manual_block(self, enabled: bool = True):\n with self._lock:\n self.state.manual_block = bool(enabled)\n if enabled:\n self.state.level = GuardrailLevel.BLOCKED\n if BlockReason.MANUAL_BLOCK not in self.state.block_reasons:\n self.state.block_reasons.append(BlockReason.MANUAL_BLOCK)\n else:\n self.state.block_reasons = [r for r in self.state.block_reasons if r != BlockReason.MANUAL_BLOCK]\n if not self.state.block_reasons:\n self.state.level = GuardrailLevel.NORMAL\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE is_manually_blocked(self) -> bool:\n with self._lock:\n return bool(self.state.manual_block) ENDREPLACEault_factory=list) ENDREPLACE
GuardrailState:\n level: GuardrailLevel = GuardrailLevel.NORMAL\n block_reasons: List[BlockReason] = field(check_error_state(self) -> tuple]:\n with self._lock:\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time < self.state.blocked_until: return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE check_error_state(self) -> tuple[bool, Optional[BlockReason]]:\n with self._lock:\n if self.state.manual_block:\n return False, BlockReason.MANUAL_BLOCK\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time() < self.state.blocked_until:\n return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE set_manual_block(self, enabled: bool = True):\n with self._lock:\n self.state.manual_block = bool(enabled)\n if enabled:\n self.state.level = GuardrailLevel.BLOCKED\n if BlockReason.MANUAL_BLOCK not in self.state.block_reasons:\n self.state.block_reasons.append(BlockReason.MANUAL_BLOCK)\n else:\n self.state.block_reasons = [r for r in self.state.block_reasons if r != BlockReason.MANUAL_BLOCK]\n if not self.state.block_reasons:\n self.state.level = GuardrailLevel.NORMAL\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE is_manually_blocked(self) -> bool:\n with self._lock:\n return bool(self.state.manual_block) ENDREPLACEault_factory=list)\n warnings: List[str] = field(check_error_state(self) -> tuple]:\n with self._lock:\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time < self.state.blocked_until: return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE check_error_state(self) -> tuple[bool, Optional[BlockReason]]:\n with self._lock:\n if self.state.manual_block:\n return False, BlockReason.MANUAL_BLOCK\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time() < self.state.blocked_until:\n return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE set_manual_block(self, enabled: bool = True):\n with self._lock:\n self.state.manual_block = bool(enabled)\n if enabled:\n self.state.level = GuardrailLevel.BLOCKED\n if BlockReason.MANUAL_BLOCK not in self.state.block_reasons:\n self.state.block_reasons.append(BlockReason.MANUAL_BLOCK)\n else:\n self.state.block_reasons = [r for r in self.state.block_reasons if r != BlockReason.MANUAL_BLOCK]\n if not self.state.block_reasons:\n self.state.level = GuardrailLevel.NORMAL\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE is_manually_blocked(self) -> bool:\n with self._lock:\n return bool(self.state.manual_block) ENDREPLACEault_factory=list)\n last_order_time: float = 0.0\n consecutive_errors: int = 0\n blocked_until: float = 0.0\n order_history: List[OrderRecord] = field(check_error_state(self) -> tuple]:\n with self._lock:\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time < self.state.blocked_until: return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE check_error_state(self) -> tuple[bool, Optional[BlockReason]]:\n with self._lock:\n if self.state.manual_block:\n return False, BlockReason.MANUAL_BLOCK\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time() < self.state.blocked_until:\n return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE set_manual_block(self, enabled: bool = True):\n with self._lock:\n self.state.manual_block = bool(enabled)\n if enabled:\n self.state.level = GuardrailLevel.BLOCKED\n if BlockReason.MANUAL_BLOCK not in self.state.block_reasons:\n self.state.block_reasons.append(BlockReason.MANUAL_BLOCK)\n else:\n self.state.block_reasons = [r for r in self.state.block_reasons if r != BlockReason.MANUAL_BLOCK]\n if not self.state.block_reasons:\n self.state.level = GuardrailLevel.NORMAL\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE is_manually_blocked(self) -> bool:\n with self._lock:\n return bool(self.state.manual_block) ENDREPLACEault_factory=list) class GuardrailState:\n level: GuardrailLevel = GuardrailLevel.NORMAL\n block_reasons: List[BlockReason] = field(check_error_state(self) -> tuple]:\n with self._lock:\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time < self.state.blocked_until: return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE check_error_state(self) -> tuple[bool, Optional[BlockReason]]:\n with self._lock:\n if self.state.manual_block:\n return False, BlockReason.MANUAL_BLOCK\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time() < self.state.blocked_until:\n return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE set_manual_block(self, enabled: bool = True):\n with self._lock:\n self.state.manual_block = bool(enabled)\n if enabled:\n self.state.level = GuardrailLevel.BLOCKED\n if BlockReason.MANUAL_BLOCK not in self.state.block_reasons:\n self.state.block_reasons.append(BlockReason.MANUAL_BLOCK)\n else:\n self.state.block_reasons = [r for r in self.state.block_reasons if r != BlockReason.MANUAL_BLOCK]\n if not self.state.block_reasons:\n self.state.level = GuardrailLevel.NORMAL\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE is_manually_blocked(self) -> bool:\n with self._lock:\n return bool(self.state.manual_block) ENDREPLACEault_factory=list)\n warnings: List[str] = field(check_error_state(self) -> tuple]:\n with self._lock:\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time < self.state.blocked_until: return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE check_error_state(self) -> tuple[bool, Optional[BlockReason]]:\n with self._lock:\n if self.state.manual_block:\n return False, BlockReason.MANUAL_BLOCK\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time() < self.state.blocked_until:\n return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE set_manual_block(self, enabled: bool = True):\n with self._lock:\n self.state.manual_block = bool(enabled)\n if enabled:\n self.state.level = GuardrailLevel.BLOCKED\n if BlockReason.MANUAL_BLOCK not in self.state.block_reasons:\n self.state.block_reasons.append(BlockReason.MANUAL_BLOCK)\n else:\n self.state.block_reasons = [r for r in self.state.block_reasons if r != BlockReason.MANUAL_BLOCK]\n if not self.state.block_reasons:\n self.state.level = GuardrailLevel.NORMAL\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE is_manually_blocked(self) -> bool:\n with self._lock:\n return bool(self.state.manual_block) ENDREPLACEault_factory=list)\n last_order_time: float = 0.0\n consecutive_errors: int = 0\n blocked_until: float = 0.0\n manual_block: bool = False\n order_history: List[OrderRecord] = field(check_error_state(self) -> tuple]:\n with self._lock:\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time < self.state.blocked_until: return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE check_error_state(self) -> tuple[bool, Optional[BlockReason]]:\n with self._lock:\n if self.state.manual_block:\n return False, BlockReason.MANUAL_BLOCK\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time() < self.state.blocked_until:\n return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE set_manual_block(self, enabled: bool = True):\n with self._lock:\n self.state.manual_block = bool(enabled)\n if enabled:\n self.state.level = GuardrailLevel.BLOCKED\n if BlockReason.MANUAL_BLOCK not in self.state.block_reasons:\n self.state.block_reasons.append(BlockReason.MANUAL_BLOCK)\n else:\n self.state.block_reasons = [r for r in self.state.block_reasons if r != BlockReason.MANUAL_BLOCK]\n if not self.state.block_reasons:\n self.state.level = GuardrailLevel.NORMAL\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE is_manually_blocked(self) -> bool:\n with self._lock:\n return bool(self.state.manual_block) ENDREPLACEault_factory=list) ENDREPLACE GuardrailState:
    level: GuardrailLevel = GuardrailLevel.NORMAL
    block_reasons: List[BlockReason] = field(check_error_state(self) -> tuple]:\n with self._lock:\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time < self.state.blocked_until: return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE check_error_state(self) -> tuple[bool, Optional[BlockReason]]:\n with self._lock:\n if self.state.manual_block:\n return False, BlockReason.MANUAL_BLOCK\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time() < self.state.blocked_until:\n return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE set_manual_block(self, enabled: bool = True):\n with self._lock:\n self.state.manual_block = bool(enabled)\n if enabled:\n self.state.level = GuardrailLevel.BLOCKED\n if BlockReason.MANUAL_BLOCK not in self.state.block_reasons:\n self.state.block_reasons.append(BlockReason.MANUAL_BLOCK)\n else:\n self.state.block_reasons = [r for r in self.state.block_reasons if r != BlockReason.MANUAL_BLOCK]\n if not self.state.block_reasons:\n self.state.level = GuardrailLevel.NORMAL\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE is_manually_blocked(self) -> bool:\n with self._lock:\n return bool(self.state.manual_block) ENDREPLACEault_factory=list)
    warnings: List[str] = field(check_error_state(self) -> tuple]:\n with self._lock:\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time < self.state.blocked_until: return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE check_error_state(self) -> tuple[bool, Optional[BlockReason]]:\n with self._lock:\n if self.state.manual_block:\n return False, BlockReason.MANUAL_BLOCK\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time() < self.state.blocked_until:\n return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE set_manual_block(self, enabled: bool = True):\n with self._lock:\n self.state.manual_block = bool(enabled)\n if enabled:\n self.state.level = GuardrailLevel.BLOCKED\n if BlockReason.MANUAL_BLOCK not in self.state.block_reasons:\n self.state.block_reasons.append(BlockReason.MANUAL_BLOCK)\n else:\n self.state.block_reasons = [r for r in self.state.block_reasons if r != BlockReason.MANUAL_BLOCK]\n if not self.state.block_reasons:\n self.state.level = GuardrailLevel.NORMAL\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE is_manually_blocked(self) -> bool:\n with self._lock:\n return bool(self.state.manual_block) ENDREPLACEault_factory=list)
    last_order_time: float = 0.0
    consecutive_errors: int = 0
    blocked_until: float = 0.0
    order_history: List[OrderRecord] = field(check_error_state(self) -> tuple]:\n with self._lock:\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time < self.state.blocked_until: return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE check_error_state(self) -> tuple[bool, Optional[BlockReason]]:\n with self._lock:\n if self.state.manual_block:\n return False, BlockReason.MANUAL_BLOCK\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time() < self.state.blocked_until:\n return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE set_manual_block(self, enabled: bool = True):\n with self._lock:\n self.state.manual_block = bool(enabled)\n if enabled:\n self.state.level = GuardrailLevel.BLOCKED\n if BlockReason.MANUAL_BLOCK not in self.state.block_reasons:\n self.state.block_reasons.append(BlockReason.MANUAL_BLOCK)\n else:\n self.state.block_reasons = [r for r in self.state.block_reasons if r != BlockReason.MANUAL_BLOCK]\n if not self.state.block_reasons:\n self.state.level = GuardrailLevel.NORMAL\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE is_manually_blocked(self) -> bool:\n with self._lock:\n return bool(self.state.manual_block) ENDREPLACEault_factory=list)

GuardrailState:\n level: GuardrailLevel = GuardrailLevel.NORMAL\n block_reasons: List[BlockReason] = field(check_error_state(self) -> tuple]:\n with self._lock:\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time < self.state.blocked_until: return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE check_error_state(self) -> tuple[bool, Optional[BlockReason]]:\n with self._lock:\n if self.state.manual_block:\n return False, BlockReason.MANUAL_BLOCK\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time() < self.state.blocked_until:\n return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE set_manual_block(self, enabled: bool = True):\n with self._lock:\n self.state.manual_block = bool(enabled)\n if enabled:\n self.state.level = GuardrailLevel.BLOCKED\n if BlockReason.MANUAL_BLOCK not in self.state.block_reasons:\n self.state.block_reasons.append(BlockReason.MANUAL_BLOCK)\n else:\n self.state.block_reasons = [r for r in self.state.block_reasons if r != BlockReason.MANUAL_BLOCK]\n if not self.state.block_reasons:\n self.state.level = GuardrailLevel.NORMAL\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE is_manually_blocked(self) -> bool:\n with self._lock:\n return bool(self.state.manual_block) ENDREPLACEault_factory=list)\n warnings: List[str] = field(check_error_state(self) -> tuple]:\n with self._lock:\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time < self.state.blocked_until: return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE check_error_state(self) -> tuple[bool, Optional[BlockReason]]:\n with self._lock:\n if self.state.manual_block:\n return False, BlockReason.MANUAL_BLOCK\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time() < self.state.blocked_until:\n return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE set_manual_block(self, enabled: bool = True):\n with self._lock:\n self.state.manual_block = bool(enabled)\n if enabled:\n self.state.level = GuardrailLevel.BLOCKED\n if BlockReason.MANUAL_BLOCK not in self.state.block_reasons:\n self.state.block_reasons.append(BlockReason.MANUAL_BLOCK)\n else:\n self.state.block_reasons = [r for r in self.state.block_reasons if r != BlockReason.MANUAL_BLOCK]\n if not self.state.block_reasons:\n self.state.level = GuardrailLevel.NORMAL\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE is_manually_blocked(self) -> bool:\n with self._lock:\n return bool(self.state.manual_block) ENDREPLACEault_factory=list)\n last_order_time: float = 0.0\n consecutive_errors: int = 0\n blocked_until: float = 0.0\n order_history: List[OrderRecord] = field(check_error_state(self) -> tuple]:\n with self._lock:\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time < self.state.blocked_until: return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE check_error_state(self) -> tuple[bool, Optional[BlockReason]]:\n with self._lock:\n if self.state.manual_block:\n return False, BlockReason.MANUAL_BLOCK\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time() < self.state.blocked_until:\n return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE set_manual_block(self, enabled: bool = True):\n with self._lock:\n self.state.manual_block = bool(enabled)\n if enabled:\n self.state.level = GuardrailLevel.BLOCKED\n if BlockReason.MANUAL_BLOCK not in self.state.block_reasons:\n self.state.block_reasons.append(BlockReason.MANUAL_BLOCK)\n else:\n self.state.block_reasons = [r for r in self.state.block_reasons if r != BlockReason.MANUAL_BLOCK]\n if not self.state.block_reasons:\n self.state.level = GuardrailLevel.NORMAL\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE is_manually_blocked(self) -> bool:\n with self._lock:\n return bool(self.state.manual_block) ENDREPLACEault_factory=list) class GuardrailState:\n level: GuardrailLevel = GuardrailLevel.NORMAL\n block_reasons: List[BlockReason] = field(check_error_state(self) -> tuple]:\n with self._lock:\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time < self.state.blocked_until: return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE check_error_state(self) -> tuple[bool, Optional[BlockReason]]:\n with self._lock:\n if self.state.manual_block:\n return False, BlockReason.MANUAL_BLOCK\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time() < self.state.blocked_until:\n return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE set_manual_block(self, enabled: bool = True):\n with self._lock:\n self.state.manual_block = bool(enabled)\n if enabled:\n self.state.level = GuardrailLevel.BLOCKED\n if BlockReason.MANUAL_BLOCK not in self.state.block_reasons:\n self.state.block_reasons.append(BlockReason.MANUAL_BLOCK)\n else:\n self.state.block_reasons = [r for r in self.state.block_reasons if r != BlockReason.MANUAL_BLOCK]\n if not self.state.block_reasons:\n self.state.level = GuardrailLevel.NORMAL\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE is_manually_blocked(self) -> bool:\n with self._lock:\n return bool(self.state.manual_block) ENDREPLACEault_factory=list)\n warnings: List[str] = field(check_error_state(self) -> tuple]:\n with self._lock:\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time < self.state.blocked_until: return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE check_error_state(self) -> tuple[bool, Optional[BlockReason]]:\n with self._lock:\n if self.state.manual_block:\n return False, BlockReason.MANUAL_BLOCK\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time() < self.state.blocked_until:\n return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE set_manual_block(self, enabled: bool = True):\n with self._lock:\n self.state.manual_block = bool(enabled)\n if enabled:\n self.state.level = GuardrailLevel.BLOCKED\n if BlockReason.MANUAL_BLOCK not in self.state.block_reasons:\n self.state.block_reasons.append(BlockReason.MANUAL_BLOCK)\n else:\n self.state.block_reasons = [r for r in self.state.block_reasons if r != BlockReason.MANUAL_BLOCK]\n if not self.state.block_reasons:\n self.state.level = GuardrailLevel.NORMAL\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE is_manually_blocked(self) -> bool:\n with self._lock:\n return bool(self.state.manual_block) ENDREPLACEault_factory=list)\n last_order_time: float = 0.0\n consecutive_errors: int = 0\n blocked_until: float = 0.0\n manual_block: bool = False\n order_history: List[OrderRecord] = field(check_error_state(self) -> tuple]:\n with self._lock:\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time < self.state.blocked_until: return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE check_error_state(self) -> tuple[bool, Optional[BlockReason]]:\n with self._lock:\n if self.state.manual_block:\n return False, BlockReason.MANUAL_BLOCK\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time() < self.state.blocked_until:\n return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE set_manual_block(self, enabled: bool = True):\n with self._lock:\n self.state.manual_block = bool(enabled)\n if enabled:\n self.state.level = GuardrailLevel.BLOCKED\n if BlockReason.MANUAL_BLOCK not in self.state.block_reasons:\n self.state.block_reasons.append(BlockReason.MANUAL_BLOCK)\n else:\n self.state.block_reasons = [r for r in self.state.block_reasons if r != BlockReason.MANUAL_BLOCK]\n if not self.state.block_reasons:\n self.state.level = GuardrailLevel.NORMAL\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE is_manually_blocked(self) -> bool:\n with self._lock:\n return bool(self.state.manual_block) ENDREPLACEault_factory=list) ENDREPLACE AIGuardrail:
    check_error_state(self) -> tuple]:\n with self._lock:\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time < self.state.blocked_until: return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE check_error_state(self) -> tuple[bool, Optional[BlockReason]]:\n with self._lock:\n if self.state.manual_block:\n return False, BlockReason.MANUAL_BLOCK\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time() < self.state.blocked_until:\n return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE set_manual_block(self, enabled: bool = True):\n with self._lock:\n self.state.manual_block = bool(enabled)\n if enabled:\n self.state.level = GuardrailLevel.BLOCKED\n if BlockReason.MANUAL_BLOCK not in self.state.block_reasons:\n self.state.block_reasons.append(BlockReason.MANUAL_BLOCK)\n else:\n self.state.block_reasons = [r for r in self.state.block_reasons if r != BlockReason.MANUAL_BLOCK]\n if not self.state.block_reasons:\n self.state.level = GuardrailLevel.NORMAL\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE is_manually_blocked(self) -> bool:\n with self._lock:\n return bool(self.state.manual_block) ENDREPLACE __init__(self, config: Optional[GuardrailConfig] = None):
        self.config = config or GuardrailConfig()
        self.state = GuardrailState()
        self._lock = threading.RLock()
        self._pending_auto_green: Dict[str, float] = {}

    check_error_state(self) -> tuple]:\n with self._lock:\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time < self.state.blocked_until: return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE check_error_state(self) -> tuple[bool, Optional[BlockReason]]:\n with self._lock:\n if self.state.manual_block:\n return False, BlockReason.MANUAL_BLOCK\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time() < self.state.blocked_until:\n return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE set_manual_block(self, enabled: bool = True):\n with self._lock:\n self.state.manual_block = bool(enabled)\n if enabled:\n self.state.level = GuardrailLevel.BLOCKED\n if BlockReason.MANUAL_BLOCK not in self.state.block_reasons:\n self.state.block_reasons.append(BlockReason.MANUAL_BLOCK)\n else:\n self.state.block_reasons = [r for r in self.state.block_reasons if r != BlockReason.MANUAL_BLOCK]\n if not self.state.block_reasons:\n self.state.level = GuardrailLevel.NORMAL\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE is_manually_blocked(self) -> bool:\n with self._lock:\n return bool(self.state.manual_block) ENDREPLACE check_market_ready(self, market_type: str) -> tuple[bool, Optional[BlockReason]]:
        if market_type in self.config.dutching_ready_market_types: return True, None
        return False, BlockReason.MARKET_NOT_READY

    check_error_state(self) -> tuple]:\n with self._lock:\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time < self.state.blocked_until: return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE check_error_state(self) -> tuple[bool, Optional[BlockReason]]:\n with self._lock:\n if self.state.manual_block:\n return False, BlockReason.MANUAL_BLOCK\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time() < self.state.blocked_until:\n return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE set_manual_block(self, enabled: bool = True):\n with self._lock:\n self.state.manual_block = bool(enabled)\n if enabled:\n self.state.level = GuardrailLevel.BLOCKED\n if BlockReason.MANUAL_BLOCK not in self.state.block_reasons:\n self.state.block_reasons.append(BlockReason.MANUAL_BLOCK)\n else:\n self.state.block_reasons = [r for r in self.state.block_reasons if r != BlockReason.MANUAL_BLOCK]\n if not self.state.block_reasons:\n self.state.level = GuardrailLevel.NORMAL\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE is_manually_blocked(self) -> bool:\n with self._lock:\n return bool(self.state.manual_block) ENDREPLACE check_wom_data(self, tick_count: int, confidence: float) -> tuple[bool, Optional[BlockReason]]:
        if tick_count < self.config.min_tick_count: return False, BlockReason.INSUFFICIENT_DATA
        if confidence < self.config.min_wom_confidence: return False, BlockReason.INSUFFICIENT_DATA
        return True, None

    check_error_state(self) -> tuple]:\n with self._lock:\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time < self.state.blocked_until: return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE check_error_state(self) -> tuple[bool, Optional[BlockReason]]:\n with self._lock:\n if self.state.manual_block:\n return False, BlockReason.MANUAL_BLOCK\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time() < self.state.blocked_until:\n return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE set_manual_block(self, enabled: bool = True):\n with self._lock:\n self.state.manual_block = bool(enabled)\n if enabled:\n self.state.level = GuardrailLevel.BLOCKED\n if BlockReason.MANUAL_BLOCK not in self.state.block_reasons:\n self.state.block_reasons.append(BlockReason.MANUAL_BLOCK)\n else:\n self.state.block_reasons = [r for r in self.state.block_reasons if r != BlockReason.MANUAL_BLOCK]\n if not self.state.block_reasons:\n self.state.level = GuardrailLevel.NORMAL\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE is_manually_blocked(self) -> bool:\n with self._lock:\n return bool(self.state.manual_block) ENDREPLACE check_volatility(self, volatility: float) -> tuple[bool, Optional[BlockReason]]:
        if volatility > self.config.max_volatility: return False, BlockReason.HIGH_VOLATILITY
        return True, None

    check_error_state(self) -> tuple]:\n with self._lock:\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time < self.state.blocked_until: return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE check_error_state(self) -> tuple[bool, Optional[BlockReason]]:\n with self._lock:\n if self.state.manual_block:\n return False, BlockReason.MANUAL_BLOCK\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time() < self.state.blocked_until:\n return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE set_manual_block(self, enabled: bool = True):\n with self._lock:\n self.state.manual_block = bool(enabled)\n if enabled:\n self.state.level = GuardrailLevel.BLOCKED\n if BlockReason.MANUAL_BLOCK not in self.state.block_reasons:\n self.state.block_reasons.append(BlockReason.MANUAL_BLOCK)\n else:\n self.state.block_reasons = [r for r in self.state.block_reasons if r != BlockReason.MANUAL_BLOCK]\n if not self.state.block_reasons:\n self.state.level = GuardrailLevel.NORMAL\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE is_manually_blocked(self) -> bool:\n with self._lock:\n return bool(self.state.manual_block) ENDREPLACE check_auto_green_grace(self, bet_id: str) -> tuple[bool, float]:
        with self._lock:
            if bet_id not in self._pending_auto_green: return True, 0.0
            elapsed = time.time() - self._pending_auto_green[bet_id]
            remaining = self.config.auto_green_grace_sec - elapsed
            if remaining <= 0:
                del self._pending_auto_green[bet_id]
                return True, 0.0
            return False, remaining

    check_error_state(self) -> tuple]:\n with self._lock:\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time < self.state.blocked_until: return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE check_error_state(self) -> tuple[bool, Optional[BlockReason]]:\n with self._lock:\n if self.state.manual_block:\n return False, BlockReason.MANUAL_BLOCK\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time() < self.state.blocked_until:\n return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE set_manual_block(self, enabled: bool = True):\n with self._lock:\n self.state.manual_block = bool(enabled)\n if enabled:\n self.state.level = GuardrailLevel.BLOCKED\n if BlockReason.MANUAL_BLOCK not in self.state.block_reasons:\n self.state.block_reasons.append(BlockReason.MANUAL_BLOCK)\n else:\n self.state.block_reasons = [r for r in self.state.block_reasons if r != BlockReason.MANUAL_BLOCK]\n if not self.state.block_reasons:\n self.state.level = GuardrailLevel.NORMAL\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE is_manually_blocked(self) -> bool:\n with self._lock:\n return bool(self.state.manual_block) ENDREPLACE register_order_for_auto_green(self, bet_id: str, placed_at: Optional[float] = None):
        with self._lock: self._pending_auto_green[bet_id] = placed_at or time.time()

    check_error_state(self) -> tuple]:\n with self._lock:\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time < self.state.blocked_until: return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE check_error_state(self) -> tuple[bool, Optional[BlockReason]]:\n with self._lock:\n if self.state.manual_block:\n return False, BlockReason.MANUAL_BLOCK\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time() < self.state.blocked_until:\n return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE set_manual_block(self, enabled: bool = True):\n with self._lock:\n self.state.manual_block = bool(enabled)\n if enabled:\n self.state.level = GuardrailLevel.BLOCKED\n if BlockReason.MANUAL_BLOCK not in self.state.block_reasons:\n self.state.block_reasons.append(BlockReason.MANUAL_BLOCK)\n else:\n self.state.block_reasons = [r for r in self.state.block_reasons if r != BlockReason.MANUAL_BLOCK]\n if not self.state.block_reasons:\n self.state.level = GuardrailLevel.NORMAL\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE is_manually_blocked(self) -> bool:\n with self._lock:\n return bool(self.state.manual_block) ENDREPLACE check_order_rate(self) -> tuple[bool, Optional[BlockReason]]:
        with self._lock:
            recent_orders = [o for o in self.state.order_history if o.timestamp > time.time() - 60.0]
            if len(recent_orders) >= self.config.max_orders_per_minute: return False, BlockReason.OVERTRADE_PROTECTION
            return True, None

    check_error_state(self) -> tuple]:\n with self._lock:\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time < self.state.blocked_until: return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE check_error_state(self) -> tuple[bool, Optional[BlockReason]]:\n with self._lock:\n if self.state.manual_block:\n return False, BlockReason.MANUAL_BLOCK\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time() < self.state.blocked_until:\n return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE set_manual_block(self, enabled: bool = True):\n with self._lock:\n self.state.manual_block = bool(enabled)\n if enabled:\n self.state.level = GuardrailLevel.BLOCKED\n if BlockReason.MANUAL_BLOCK not in self.state.block_reasons:\n self.state.block_reasons.append(BlockReason.MANUAL_BLOCK)\n else:\n self.state.block_reasons = [r for r in self.state.block_reasons if r != BlockReason.MANUAL_BLOCK]\n if not self.state.block_reasons:\n self.state.level = GuardrailLevel.NORMAL\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE is_manually_blocked(self) -> bool:\n with self._lock:\n return bool(self.state.manual_block) ENDREPLACE check_error_state(self) -> tuple[bool, Optional[BlockReason]]:
        with self._lock:
            if self.state.consecutive_errors >= self.config.consecutive_error_limit:
                if time.time() < self.state.blocked_until: return False, BlockReason.CONSECUTIVE_ERRORS
                else:
                    self.state.consecutive_errors = 0
                    self.state.blocked_until = 0.0
            return True, None

    check_error_state(self) -> tuple]:\n with self._lock:\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time < self.state.blocked_until: return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE check_error_state(self) -> tuple[bool, Optional[BlockReason]]:\n with self._lock:\n if self.state.manual_block:\n return False, BlockReason.MANUAL_BLOCK\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time() < self.state.blocked_until:\n return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE set_manual_block(self, enabled: bool = True):\n with self._lock:\n self.state.manual_block = bool(enabled)\n if enabled:\n self.state.level = GuardrailLevel.BLOCKED\n if BlockReason.MANUAL_BLOCK not in self.state.block_reasons:\n self.state.block_reasons.append(BlockReason.MANUAL_BLOCK)\n else:\n self.state.block_reasons = [r for r in self.state.block_reasons if r != BlockReason.MANUAL_BLOCK]\n if not self.state.block_reasons:\n self.state.level = GuardrailLevel.NORMAL\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE is_manually_blocked(self) -> bool:\n with self._lock:\n return bool(self.state.manual_block) ENDREPLACE record_order(self, market_id: str, selection_id: int, side: str, stake: float, success: bool = True):
        with self._lock:
            self.state.order_history.append(OrderRecord(time.time(), market_id, selection_id, side, stake, success))
            if len(self.state.order_history) > 100: self.state.order_history = self.state.order_history[-100:]
            if success: self.state.consecutive_errors = 0
            else:
                self.state.consecutive_errors += 1
                if self.state.consecutive_errors >= self.config.consecutive_error_limit:
                    self.state.blocked_until = time.time() + self.config.cooldown_after_error_sec

    check_error_state(self) -> tuple]:\n with self._lock:\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time < self.state.blocked_until: return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE check_error_state(self) -> tuple[bool, Optional[BlockReason]]:\n with self._lock:\n if self.state.manual_block:\n return False, BlockReason.MANUAL_BLOCK\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time() < self.state.blocked_until:\n return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE set_manual_block(self, enabled: bool = True):\n with self._lock:\n self.state.manual_block = bool(enabled)\n if enabled:\n self.state.level = GuardrailLevel.BLOCKED\n if BlockReason.MANUAL_BLOCK not in self.state.block_reasons:\n self.state.block_reasons.append(BlockReason.MANUAL_BLOCK)\n else:\n self.state.block_reasons = [r for r in self.state.block_reasons if r != BlockReason.MANUAL_BLOCK]\n if not self.state.block_reasons:\n self.state.level = GuardrailLevel.NORMAL\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE is_manually_blocked(self) -> bool:\n with self._lock:\n return bool(self.state.manual_block) ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:
        with self._lock:
            reasons = []
            warnings = []

            ok, reason = self.check_market_ready(market_type)
            if not ok: reasons.append(reason)
            ok, reason = self.check_wom_data(tick_count, wom_confidence)
            if not ok: reasons.append(reason)

            # FIX: VOLATILITA DIVENTA HARD BLOCK
            ok, reason = self.check_volatility(volatility)
            if not ok: reasons.append(reason)

            ok, reason = self.check_order_rate()
            if not ok: reasons.append(reason)
            ok, reason = self.check_error_state()
            if not ok: reasons.append(reason)

            if reasons:
                level, can_proceed = GuardrailLevel.BLOCKED, False
            elif warnings:
                level, can_proceed = GuardrailLevel.WARNING, True
            else:
                level, can_proceed = GuardrailLevel.NORMAL, True

            self.state.level = level
            self.state.block_reasons = reasons
            self.state.warnings = warnings

            return {
                "can_proceed": can_proceed, "level": level.value,
                "reasons": [r.value for r in reasons], "warnings": warnings,
                "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0
            }

    check_error_state(self) -> tuple]:\n with self._lock:\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time < self.state.blocked_until: return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE check_error_state(self) -> tuple[bool, Optional[BlockReason]]:\n with self._lock:\n if self.state.manual_block:\n return False, BlockReason.MANUAL_BLOCK\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time() < self.state.blocked_until:\n return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE set_manual_block(self, enabled: bool = True):\n with self._lock:\n self.state.manual_block = bool(enabled)\n if enabled:\n self.state.level = GuardrailLevel.BLOCKED\n if BlockReason.MANUAL_BLOCK not in self.state.block_reasons:\n self.state.block_reasons.append(BlockReason.MANUAL_BLOCK)\n else:\n self.state.block_reasons = [r for r in self.state.block_reasons if r != BlockReason.MANUAL_BLOCK]\n if not self.state.block_reasons:\n self.state.level = GuardrailLevel.NORMAL\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE is_manually_blocked(self) -> bool:\n with self._lock:\n return bool(self.state.manual_block) ENDREPLACE get_auto_green_delay(self, bet_id: str) -> float:
        can_green, remaining = self.check_auto_green_grace(bet_id)
        return remaining if not can_green else 0.0

    check_error_state(self) -> tuple]:\n with self._lock:\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time < self.state.blocked_until: return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE check_error_state(self) -> tuple[bool, Optional[BlockReason]]:\n with self._lock:\n if self.state.manual_block:\n return False, BlockReason.MANUAL_BLOCK\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time() < self.state.blocked_until:\n return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE set_manual_block(self, enabled: bool = True):\n with self._lock:\n self.state.manual_block = bool(enabled)\n if enabled:\n self.state.level = GuardrailLevel.BLOCKED\n if BlockReason.MANUAL_BLOCK not in self.state.block_reasons:\n self.state.block_reasons.append(BlockReason.MANUAL_BLOCK)\n else:\n self.state.block_reasons = [r for r in self.state.block_reasons if r != BlockReason.MANUAL_BLOCK]\n if not self.state.block_reasons:\n self.state.level = GuardrailLevel.NORMAL\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE is_manually_blocked(self) -> bool:\n with self._lock:\n return bool(self.state.manual_block) ENDREPLACE reset(self):
        with self._lock:
            self.state = GuardrailState()
            self._pending_auto_green.clear()

    check_error_state(self) -> tuple]:\n with self._lock:\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time < self.state.blocked_until: return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE check_error_state(self) -> tuple[bool, Optional[BlockReason]]:\n with self._lock:\n if self.state.manual_block:\n return False, BlockReason.MANUAL_BLOCK\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time() < self.state.blocked_until:\n return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE set_manual_block(self, enabled: bool = True):\n with self._lock:\n self.state.manual_block = bool(enabled)\n if enabled:\n self.state.level = GuardrailLevel.BLOCKED\n if BlockReason.MANUAL_BLOCK not in self.state.block_reasons:\n self.state.block_reasons.append(BlockReason.MANUAL_BLOCK)\n else:\n self.state.block_reasons = [r for r in self.state.block_reasons if r != BlockReason.MANUAL_BLOCK]\n if not self.state.block_reasons:\n self.state.level = GuardrailLevel.NORMAL\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE is_manually_blocked(self) -> bool:\n with self._lock:\n return bool(self.state.manual_block) ENDREPLACE get_status(self) -> Dict:
        with self._lock:
            now = time.time()
            recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]
            return {
                "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,
                "orders_last_minute": len(recent_orders), "pending_auto_green": len(self._pending_auto_green),
                "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,
                "warnings": self.state.warnings, "block_reasons": [r.value for r in self.state.block_reasons]
            }

_global_guardrail: Optional[AIGuardrail] = None
check_error_state(self) -> tuple]:\n with self._lock:\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time < self.state.blocked_until: return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE check_error_state(self) -> tuple[bool, Optional[BlockReason]]:\n with self._lock:\n if self.state.manual_block:\n return False, BlockReason.MANUAL_BLOCK\n if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n if time.time() < self.state.blocked_until:\n return False, BlockReason.CONSECUTIVE_ERRORS\n else:\n self.state.consecutive_errors = 0\n self.state.blocked_until = 0.0\n return True, None\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE set_manual_block(self, enabled: bool = True):\n with self._lock:\n self.state.manual_block = bool(enabled)\n if enabled:\n self.state.level = GuardrailLevel.BLOCKED\n if BlockReason.MANUAL_BLOCK not in self.state.block_reasons:\n self.state.block_reasons.append(BlockReason.MANUAL_BLOCK)\n else:\n self.state.block_reasons = [r for r in self.state.block_reasons if r != BlockReason.MANUAL_BLOCK]\n if not self.state.block_reasons:\n self.state.level = GuardrailLevel.NORMAL\n\n full_check -> Dict:\n with self._lock:\n reasons = \n if not ok: reasons.append\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed, "level": level.value,\n "reasons": , "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n } reset:\n with self._lock:\n self.state = GuardrailState\n self._pending_auto_green.clear get_status -> Dict:\n with self._lock:\n now = time.time\n recent_orders = \n return {\n "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len, "pending_auto_green": len,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings, "block_reasons": \n } def get_status(self) -> Dict:\n with self._lock:\n now = time.time()\n recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n return {\n "level": self.state.level.value,\n "consecutive_errors": self.state.consecutive_errors,\n "orders_last_minute": len(recent_orders),\n "pending_auto_green": len(self._pending_auto_green),\n "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n "warnings": self.state.warnings,\n "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n "manual_block": bool(self.state.manual_block),\n } ENDREPLACE reset(self):\n with self._lock:\n self.state = GuardrailState()\n self._pending_auto_green.clear() ENDREPLACE full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n with self._lock:\n reasons = []\n warnings = []\n\n ok, reason = self.check_market_ready(market_type)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_wom_data(tick_count, wom_confidence)\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_volatility(volatility)\n if not ok:\n warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n ok, reason = self.check_order_rate()\n if not ok:\n reasons.append(reason)\n\n ok, reason = self.check_error_state()\n if not ok:\n reasons.append(reason)\n\n if reasons:\n level, can_proceed = GuardrailLevel.BLOCKED, False\n elif warnings:\n level, can_proceed = GuardrailLevel.WARNING, True\n else:\n level, can_proceed = GuardrailLevel.NORMAL, True\n\n self.state.level = level\n self.state.block_reasons = reasons\n self.state.warnings = warnings\n\n return {\n "can_proceed": can_proceed,\n "level": level.value,\n "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n "warnings": warnings,\n "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n } ENDREPLACE is_manually_blocked(self) -> bool:\n with self._lock:\n return bool(self.state.manual_block) ENDREPLACE get_guardrail() -> AIGuardrail:
    global _global_guardrail
    if _global_guardrail is None: _global_guardrail = AIGuardrail()
    return _global_guardrail


name: AI Guardrails

on:
  workflow_dispatch:
  push:
    branches:
      - main
  pull_request:

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Test workflow loads
        run: echo "AI Guardrails workflow is valid"
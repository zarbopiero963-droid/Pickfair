name: Pickfair Test Suite

on:
  push:
    branches: ["main", "master"]
  pull_request:
    branches: ["main", "master"]
  workflow_dispatch:

jobs:
  esegui-test:
    runs-on: ubuntu-latest
    env:
      PYTHONUNBUFFERED: "1"
      PYTHONPATH: ${{ github.workspace }}

    steps:
      - name: Scarica il codice
        uses: actions/checkout@v4

      - name: Configura Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Installa dipendenze di sistema
        run: |
          sudo apt-get update
          sudo apt-get install -y xvfb libx11-xcb1 libxrender1 libxext6 libx11-6 libgl1

      - name: Installa dipendenze Python
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
          if [ -f requirements-dev.txt ]; then
            pip install -r requirements-dev.txt
          else
            pip install pytest pytest-cov pytest-asyncio pytest-mock
          fi

          # safety net per workflow che importano telegram_listener
          python -m pip install telethon customtkinter || true

      - name: Debug package AI
        run: |
          python - <<'PY'
          import os
          import sys

          print("cwd =", os.getcwd())
          print("python =", sys.executable)
          print("sys.path[0:10] =", sys.path[:10])
          print("ai exists =", os.path.isdir("ai"))
          print("ai/__init__.py exists =", os.path.isfile("ai/__init__.py"))
          print("ai/ai_guardrail.py exists =", os.path.isfile("ai/ai_guardrail.py"))
          PY

      - name: Lancia Test Compatibili (Hedge-Fund V2)
        run: |
          xvfb-run -a python -m pytest tests/ \
            --ignore=tests/test_dutching_safety.py \
            --ignore=tests/test_performance.py \
            --ignore=tests/test_dutching_advanced.py \
            --ignore=tests/test_liquidity_guard.py \
            --ignore=tests/test_new_components.py \
            --ignore=tests/test_simulation_mode.py \
            --ignore=tests/test_telemetry_indicator.py \
            --ignore=tests/test_toolbar_live.py \
            --ignore=tests/test_ui_components.py \
            -v

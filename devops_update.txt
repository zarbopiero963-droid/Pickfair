REPLACE ai/ai_guardrail.py class GuardrailState:\n    level: GuardrailLevel = GuardrailLevel.NORMAL\n    block_reasons: List[BlockReason] = field(default_factory=list)\n    warnings: List[str] = field(default_factory=list)\n    last_order_time: float = 0.0\n    consecutive_errors: int = 0\n    blocked_until: float = 0.0\n    order_history: List[OrderRecord] = field(default_factory=list) class GuardrailState:\n    level: GuardrailLevel = GuardrailLevel.NORMAL\n    block_reasons: List[BlockReason] = field(default_factory=list)\n    warnings: List[str] = field(default_factory=list)\n    last_order_time: float = 0.0\n    consecutive_errors: int = 0\n    blocked_until: float = 0.0\n    manual_block: bool = False\n    order_history: List[OrderRecord] = field(default_factory=list) ENDREPLACE

REPLACE ai/ai_guardrail.py def check_error_state(self) -> tuple]:\n        with self._lock:\n            if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n                if time.time < self.state.blocked_until: return False, BlockReason.CONSECUTIVE_ERRORS\n                else:\n                    self.state.consecutive_errors = 0\n                    self.state.blocked_until = 0.0\n            return True, None def check_error_state(self) -> tuple[bool, Optional[BlockReason]]:\n        with self._lock:\n            if self.state.manual_block:\n                return False, BlockReason.MANUAL_BLOCK\n            if self.state.consecutive_errors >= self.config.consecutive_error_limit:\n                if time.time() < self.state.blocked_until:\n                    return False, BlockReason.CONSECUTIVE_ERRORS\n                else:\n                    self.state.consecutive_errors = 0\n                    self.state.blocked_until = 0.0\n            return True, None\n\n    def set_manual_block(self, enabled: bool = True):\n        with self._lock:\n            self.state.manual_block = bool(enabled)\n            if enabled:\n                self.state.level = GuardrailLevel.BLOCKED\n                if BlockReason.MANUAL_BLOCK not in self.state.block_reasons:\n                    self.state.block_reasons.append(BlockReason.MANUAL_BLOCK)\n            else:\n                self.state.block_reasons = [r for r in self.state.block_reasons if r != BlockReason.MANUAL_BLOCK]\n                if not self.state.block_reasons:\n                    self.state.level = GuardrailLevel.NORMAL\n\n    def is_manually_blocked(self) -> bool:\n        with self._lock:\n            return bool(self.state.manual_block) ENDREPLACE

REPLACE ai/ai_guardrail.py def full_check -> Dict:\n        with self._lock:\n            reasons = \n            if not ok: reasons.append\n\n            if reasons:\n                level, can_proceed = GuardrailLevel.BLOCKED, False\n            elif warnings:\n                level, can_proceed = GuardrailLevel.WARNING, True\n            else:\n                level, can_proceed = GuardrailLevel.NORMAL, True\n\n            self.state.level = level\n            self.state.block_reasons = reasons\n            self.state.warnings = warnings\n\n            return {\n                "can_proceed": can_proceed, "level": level.value,\n                "reasons": , "warnings": warnings,\n                "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time else 0\n            } def full_check(self, market_type: str, tick_count: int = 0, wom_confidence: float = 0.5, volatility: float = 0.0) -> Dict:\n        with self._lock:\n            reasons = []\n            warnings = []\n\n            ok, reason = self.check_market_ready(market_type)\n            if not ok:\n                reasons.append(reason)\n\n            ok, reason = self.check_wom_data(tick_count, wom_confidence)\n            if not ok:\n                reasons.append(reason)\n\n            ok, reason = self.check_volatility(volatility)\n            if not ok:\n                warnings.append(reason.value if hasattr(reason, "value") else str(reason))\n\n            ok, reason = self.check_order_rate()\n            if not ok:\n                reasons.append(reason)\n\n            ok, reason = self.check_error_state()\n            if not ok:\n                reasons.append(reason)\n\n            if reasons:\n                level, can_proceed = GuardrailLevel.BLOCKED, False\n            elif warnings:\n                level, can_proceed = GuardrailLevel.WARNING, True\n            else:\n                level, can_proceed = GuardrailLevel.NORMAL, True\n\n            self.state.level = level\n            self.state.block_reasons = reasons\n            self.state.warnings = warnings\n\n            return {\n                "can_proceed": can_proceed,\n                "level": level.value,\n                "reasons": [r.value if hasattr(r, "value") else str(r) for r in reasons],\n                "warnings": warnings,\n                "blocked_until": self.state.blocked_until if self.state.blocked_until > time.time() else 0,\n            } ENDREPLACE

REPLACE ai/ai_guardrail.py def reset:\n        with self._lock:\n            self.state = GuardrailState\n            self._pending_auto_green.clear def reset(self):\n        with self._lock:\n            self.state = GuardrailState()\n            self._pending_auto_green.clear() ENDREPLACE

REPLACE ai/ai_guardrail.py def get_status -> Dict:\n        with self._lock:\n            now = time.time\n            recent_orders = \n            return {\n                "level": self.state.level.value, "consecutive_errors": self.state.consecutive_errors,\n                "orders_last_minute": len, "pending_auto_green": len,\n                "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n                "warnings": self.state.warnings, "block_reasons": \n            } def get_status(self) -> Dict:\n        with self._lock:\n            now = time.time()\n            recent_orders = [o for o in self.state.order_history if o.timestamp > now - 60.0]\n            return {\n                "level": self.state.level.value,\n                "consecutive_errors": self.state.consecutive_errors,\n                "orders_last_minute": len(recent_orders),\n                "pending_auto_green": len(self._pending_auto_green),\n                "blocked_until": self.state.blocked_until if self.state.blocked_until > now else 0,\n                "warnings": self.state.warnings,\n                "block_reasons": [r.value if hasattr(r, "value") else str(r) for r in self.state.block_reasons],\n                "manual_block": bool(self.state.manual_block),\n            } ENDREPLACE